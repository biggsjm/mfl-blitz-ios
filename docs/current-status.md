# Current app status

## September 17: build 71 — data status release in progress

The scoring freshness control now opens a compact Data status sheet for MFL scores, NFL player stats, background Live Activity updates and enabled lineup alerts. Each row uses an icon plus a short status, with an expandable reason and the actual source receipt where available. Projection help is separate. Largest accessibility text opens a full-height sheet. Status is scoped to the viewed week, matchup or player; it is not a provider-wide uptime claim.

NFL status checks expected data against MFL-confirmed play. Successful responses with an unstarted game, empty stats, stale boxes or missing scored players report Delayed. Completed boxes keep their source receipt without expiring just with age, and inactive games report Idle. Missing or unknown game information remains Checking. Background and alert rows describe saved registration/permission/expiry evidence, not guaranteed notification delivery. No provider requests, health polling, credentials, service roles or backend configuration are added or changed.

Player details retain official MFL points while stats are missing and omit the misleading computed zero total and unexplained-points arithmetic until usable stats exist. Local app regression tests pass (412 Swift Testing cases and four XCTest layout checks in `/tmp/mfl71-status-validation.xcresult`). Final status rules pass all 14 cases; normal/largest-text status navigation and existing points/timeline UI journeys pass in `/tmp/mfl71-final-validation.xcresult`. The missing-stats journey passes in `/tmp/mfl71-missing-stats.xcresult` after correcting its assertion to match iOS’s combined points label. Native screenshots in `/tmp/mfl71-final-shots` verify the compact and accessible layouts. Build-71 distribution is in progress. Build 70 remains the released build until delivery is verified below. Unrelated synchronized duplicate files remain untouched.

## September 17: build 70 — available in TestFlight and merged

**0.7.0 (70) is Testing** for Owner Testing (one internal tester) and Champion Hall Owners (12 existing external testers), with **Automatically notify testers** enabled. Apple finished processing, the unchanged Apple-only encryption answer is saved, and build-specific What to Test notes are saved. App Store Connect build ID: `ab270b76-dcbc-4841-8cc5-135e96258928`. Upload succeeded at **19:54:12 CDT**; both groups were verified before merge. Build 70 phone installation remains unverified.

Release source is `aaf74f94b4bc4abbf63b6590218ea132dbe1b4c7`. [PR #16](https://github.com/biggsjm/mfl-blitz-ios/pull/16) merged at **20:03:38 CDT** as `3d140b5d9987249ec90ca6752514e1dc29283f8e`, after all six jobs passed in [CI run 35292090614](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/35292090614): core, app, background scoring and all three native UI groups. Local native and visual evidence is recorded below. The approved archive/export and upload log are `/tmp/MFLBlitz-70-approved.xcarchive`, `/tmp/mfl70-approved-export/MFLBlitz.ipa`, `/tmp/mfl70-approved-upload-beta.log`; signed-export evidence is `/tmp/mfl70-approved-export-evidence.json`.

This release contains compact team rows on Scores, side-by-side team columns above matchup players, aligned owner/record/score rows, accessible text layouts, and the delayed-NFL-kickoff fallback. The Live Activity layout/colors and backend services remain unchanged. Earlier build-70 checkpoints below are historical; their pending/hold states are superseded by this delivery.

## September 17: build 70 — approved layout and release resumed

Josh approved the comparison preview and requested TestFlight upload followed by merge. The full matchup now uses side-by-side team columns, with aligned owner/record/score rows directly above the mirrored players. Scores keeps option B's compact team rows. Live players retains its compact pinned totals and explicit team headings. The delayed-NFL-kickoff fallback below is included. Larger text retains the readable stacked-metric adaptation. The earlier candidate export is superseded; native validation and a new archive are in progress. No upload or merge has occurred at this checkpoint.

Local validation passes in `/tmp/mfl70-approved.xcresult`: 399 Swift Testing cases, four existing XCTest layout checks, a temporary render harness and four native UI journeys (408 reported tests, zero failures/runtime warnings). The journeys cover overview density/player rows, Live players/navigation, largest-text timeline/points breakdown and owner/record/score alignment. Native light/dark upcoming/live/final/stale/long-name/large-text captures are in `/tmp/mfl70-approved-shots`. The final badge-spacing refinement and alignment journey also pass in `/tmp/mfl70-badge-layout.xcresult`; captures in `/tmp/mfl70-badge-shots` verify positive/negative changes remain clear of projections and stale data suppresses them. Pre-existing untracked synchronized duplicate files remain untouched and excluded from verification/release snapshots.

Release source `aaf74f94b4bc4abbf63b6590218ea132dbe1b4c7` is pushed on `release/testflight-0.7.0-70`, with [PR #16](https://github.com/biggsjm/mfl-blitz-ios/pull/16) open. The rebuilt archive `/tmp/MFLBlitz-70-approved.xcarchive` and `/tmp/mfl70-approved-export/MFLBlitz.ipa` pass distribution verification (`/tmp/mfl70-approved-verification.log`, `/tmp/mfl70-approved-export-evidence.json`). After Josh unlocked the Mac and signed in to Xcode, upload succeeded at **19:54:12 CDT** (`/tmp/mfl70-approved-upload-beta.log`). The archive was built with stable Xcode 27; the successful upload used the Mac's selected Xcode 27 beta installation. Apple processing is pending. [CI run 35292090614](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/35292090614) is in progress. TestFlight assignment and merge remain pending.

## September 17: unreleased — delayed NFL kickoff fallback

Josh reported both fantasy points and NFL game information missing just after Bills–Lions started, then confirmed scores had appeared. Read-only checks found the NFL worker running without a quota pause or failures. API-NFL's cached Week 2 game still reported `NS`, no clock and null scores at **19:25:07** and **19:29:50 CDT**; there was no player box yet. At **19:26:19 CDT**, MFL's public live-scoring export already contained fantasy points and running player clocks, while its NFL schedule export still had empty scores. These observations establish a source delay; they do not establish exactly when the first fantasy points became available to the phone. The NFL worker used 41 requests after UTC midnight, far below its 6,000-request ceiling.

The app also gave an NFL `NS` response precedence over MFL's running clock. Local changes now preserve MFL-confirmed live status, active-player grouping and the available opponent until NFL context catches up; they suppress pregame projections for those active players. Missing NFL scores/stat lines remain missing, and kickoff time or fantasy points alone cannot establish live play. Existing halftime/overtime/exceptional-state handling and request cadence/budgets are retained. Targeted NFL feed/store, matchup-mode and scoring-refresh tests pass on iOS 18.4 in `/tmp/mfl70-kickoff-fallback.xcresult`. No service, schedule or access configuration changed.

The requested comparison preview is `/Users/joshbiggs/.codex/visualizations/2026/09/13/01a09af9-47a5-7861-b4cd-9848d4741eee/matchup-overview-and-live.html`: compact B rows on Scores and a side-by-side header above live player columns. It contains illustrative data and is not yet the implemented detail layout. **The previously exported build 70 is superseded by the scoring fix and must be rebuilt before upload.** Design review and distribution remain pending.

## September 17: build 70 — release preparation

**Upload on hold:** after the signed export passed verification, Josh questioned how stacked summary rows relate to the live matchup's left/right player columns. The proposed revision keeps the compact card on Scores and restores a compact side-by-side summary on matchup detail; the layout decision is pending. Nothing has been uploaded, pushed or merged for build 70. The signed candidate below must be rebuilt if the design changes.

Candidate source is `e521667e9ea7ff2400ff3555abb21f97c7045346` on `release/testflight-0.7.0-70`. Archive `/tmp/MFLBlitz-70.xcarchive` and export `/tmp/mfl70-export/MFLBlitz.ipa` pass strict signatures, app/widget version, distribution profiles, production push and gateway/privacy checks. Evidence: `/tmp/mfl70-export-verification.log`, `/tmp/mfl70-export-evidence.json`. Subsequent scrolling-helper edits affect tests only. The delayed-caption journey passes on iOS 18.4 in `/tmp/mfl70-compat.xcresult` (that run also reproduced the lineup helper failure). Both revised largest-text lineup/roster journeys pass in `/tmp/mfl70-scroll-fix.xcresult`. The lineup helper now scrolls within unobscured list content above the pinned review button and reverses if it passes the target; roster scrolling uses measured content bounds and still requires a fully visible action. These results resolve the reproduced local failures; a new full CI run has not started.

Josh authorized TestFlight distribution followed by push/merge. Build 70 packages the approved compact matchup summary described below; app and Live Activity versions are both 0.7.0 (70). The distribution archive, export verification, Apple processing and tester assignment are pending at this checkpoint. Build 69 remains the installed/available beta baseline (phone installation is unverified).

The previous build-69 post-merge CI run has completed: core, app and backend tests passed; three native UI journeys failed. The delayed-game-caption assertion still expected opponent-first text despite the approved day-first format, and the largest-text lineup/roster helpers did not reach their target controls. The caption assertion is updated; older-iOS verification of all three journeys is in progress. Do not treat the earlier pending CI entry below as a passing result.

## September 17: unreleased — compact matchup card (option B)

Josh selected the compact team-row design. Scores' featured matchup and the matchup detail summary now share aligned Points and Proj./Live est. columns, with owner, record and known playing/to-play counts beneath each team. A shared footer shows the next known kickoff or current matchup state. Final matchups hide projections, stale progress is suppressed, and Live Activity receipt reconciliation and point-change indicators are preserved. At XX Large and larger text sizes, metrics move beneath the team identity to avoid squeezing long names and precise scores.

Validation: 17 existing scoring/receipt model tests and both affected native UI journeys pass in `/tmp/mfl-matchup-b-clean.xcresult`. After the large-text adjustment, both UI journeys pass again in `/tmp/mfl-matchup-b-final.xcresult`, along with a temporary native render harness producing 12 light/dark captures for upcoming, live, final, stale, long-name/three-decimal and largest-accessibility states. Captures were inspected in `/tmp/mfl-matchup-b-final-shots`. The harness exists only in the temporary verification copy; it is not app source. Checks used a copy of tracked source with current edits because pre-existing untracked duplicate Swift files break direct workspace package compilation; those files are untouched. Manual VoiceOver/device validation remains pending.

Source changes are local and unreleased. TestFlight remains **0.7.0 (69)**. No additional data requests, refresh subscriptions, service changes, commit/push or distribution action occurred.

## September 17: build 69 — merged and available in TestFlight

Josh authorized committing, pushing/merging and uploading the collected changes. **0.7.0 (69)** includes the approved shared player-row layout, cached jersey metadata, sorted bench and consistent trailing Search control. [PR #15](https://github.com/biggsjm/mfl-blitz-ios/pull/15) merged to `main` as `2d788e48c7731f6913f3c2467ac6f31c72a61416` at **18:20:45 CDT**. The archive's application source is `5b5a6bf780675948950d1b47fbdcd55c59ee7fbd`; subsequent changes are tests/documentation only.

The stable-Xcode archive and App Store export pass strict signatures, matching app/widget versions, distribution profiles, production push entitlement and gateway/privacy checks. Upload succeeded at **18:04:12 CDT**. Apple processing and the Apple-only encryption declaration are complete; build-specific What to Test notes are saved. Build ID: `67386447-8d00-4867-ae5c-b6d568845807`. The build is assigned to **Owner Testing (one tester)** and **Champion Hall Owners (12 testers)**, with automatic notification selected. App Store Connect reports **Testing**, including the external group's build page, with 13 invitations. Phone installation of build 69 remains unverified. No new testers, public link, service or backend-access changes were made.

The initial CI run passed **399 app checks** (395 Swift Testing cases plus four XCTest render checks), **115 core tests**, and background-service/gateway tests. The player-row work also passed 58 focused model checks and 10 native UI journeys before release. Three older name-only assertions needed to verify the combined player identity, and the cold-link UI helper needed to dismiss the expected unsigned-simulator secure-storage alert before checking Preview. All four affected native journeys now pass: `/tmp/mfl69-heading-compatibility.xcresult` and `/tmp/mfl69-legacy-navigation.xcresult` (the latter on iOS 18.4). Only tests/documentation changed after archive; the application binary is unchanged. The full post-merge CI rerun is **still running**, not yet claimed passed: [run 35286418976](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/35286418976).

Artifacts: `/tmp/MFLBlitz-69.xcarchive`, `/tmp/mfl69-export/MFLBlitz.ipa`, `/tmp/mfl69-export-verification.log`, `/tmp/mfl69-export-evidence.json`, `/tmp/mfl69-upload.log`. App Store Connect also confirms the previous build 68 is now Testing with three installations; this is a later observation than its September 16 review checkpoint below.

## September 17: build 69 implementation — shared player identity and day-first game information

Josh approved the native preview. Player rows now place NFL team and an available jersey number beside the name, followed by day/time, opponent and trailing injury/lock status. The shared layout covers Lineup and replacement/review choices, roster management, search, watchlists, waivers, trade player assets, player details and matchup rows. Ownership remains separate in search, and live/final matchup captions retain the score and clock. Largest accessibility text gives lineup names/game information the full row width and moves projection/action controls below; compact matchup rows preserve their equal areas.

MFL's basic player directory omits jersey numbers, so settled player lists request optional metadata through cached detailed-player batches of at most 100 IDs. Reads are shared, successful/missing results are retained for 24 hours in the session, failures back off for five minutes, and account changes discard old responses. Existing persistent MFL caching is also reused. Number 0 is supported; missing/invalid numbers or numbers for a different NFL team are hidden. These reads do not block screen loading, force refreshes, add paid NFL-provider requests or create a polling loop. See [player layout and request policy](player-detail-ux.md).

Validation passes for **58 focused app model tests and 10 native UI journeys**, covering cache/request boundaries and account safety, normal/largest-text lineup actions, live players, search and ownership/navigation, watchlists/player detail, compact/equal matchup rows, trading block and free-agent review/cancellation. Results: `/tmp/mfl-player-rows-validation.xcresult` and `/tmp/mfl-player-rows-tools.xcresult`; screenshots are exported to the matching `-shots` directories. The preview's largest-text scrolling failure is resolved by the full-width accessibility layout. This implementation checkpoint made no service changes or upload. Build 69 distribution is recorded above.

## September 17: build 69 implementation — sorted lineup bench

The Lineup bench now follows the league's position order, then descending weekly projection within each position. Missing projections come last within their position; names and player IDs resolve ties. The same order applies to the bench-tiebreaker menu. Sorting is computed for display and does not change the underlying roster, draft membership or submission payload.

The simulator build, all seven existing `LineupBenchFlowTests` (including parameterized safety cases), and the native full-bench Start/cancel/reopen/FLEX-swap/review journey pass in `/tmp/mfl-bench-order.xcresult`. This implementation checkpoint made no TestFlight upload; the fix is now included in build 69 as recorded above.

## September 16: unreleased — consistent search toolbar position

Player search now owns one ordered trailing toolbar group: the screen's Week, compose, standings information or matchup timeline action comes first, with Search last. Close search occupies the same far-right position. My Team continues to use the same shared search control, and leading Settings/lineup alerts stay in place. This removes modifier-order differences that put Search before information or timeline on some screens.

All four existing native UI checks pass in `/tmp/mfl-toolbar-order.xcresult`: search on every tab with player Back/query retention, matchup search return, standings information and lineup week selection. The exported screenshots in `/tmp/mfl-toolbar-order-shots` confirm the trailing order on Standings, matchup, Lineup and open search. The final simulator build passes after preserving Scores' existing inline-title modifier order (`/tmp/mfl-toolbar-final-build.log`). Interactive desktop inspection was unavailable while the Mac was locked; test screenshots provide the visual evidence. Josh authorized source integration in [PR #14](https://github.com/biggsjm/mfl-blitz-ios/pull/14) and requested collecting more changes before another TestFlight build. The toolbar fix remains unreleased; TestFlight build 68 remains unchanged.

## September 16: build 68 — owner TestFlight available; league beta awaiting review

Josh authorized a new TestFlight release. **0.7.0 (68)** includes the saved-login startup fix and player-search relevance and dismissal changes described below. All 22 search model tests and 10 native search journeys passed; the startup fix passed its model and native checks before merge. The stable-Xcode archive and App Store export pass strict signatures, matching app/widget versions, distribution profiles, production push entitlement and existing gateway configuration. Archive source is `2b47c6a6a610fad9dc2c9c23bdaa9f7fc0f143b2`, pushed on `release/testflight-0.7.0-68` and available in [PR #14](https://github.com/biggsjm/mfl-blitz-ios/pull/14).

After Josh reauthenticated Xcode, upload succeeded at **17:11:13 CDT**. Apple processing completed and export compliance cleared after a build-specific source/binary check confirmed only Apple OS cryptography (URLSession HTTPS, Security/Keychain and CryptoKit), no external package dependencies or embedded third-party frameworks. Build-specific What to Test notes are saved. App Store Connect build ID: `d85b889b-808f-4fa2-8992-80925965f213`.

With Josh's explicit approval, build 68 is assigned to **Owner Testing (one internal tester)** and **Champion Hall Owners (12 external testers)**. Owner Testing confirms **Testing** for build 68. Its external status is **Waiting for Review**; **Automatically notify testers** was checked when submitting. Build 67 was withdrawn and is now **Ready to Submit**. The 12 external testers will receive access after Apple's approval. No new testers, public link, service or backend access configuration were added. Build 68 phone installation remains unverified.

All six CI jobs pass in [replacement run 35157343449](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/35157343449): core, full app, background service and the three native UI groups. The original source run's native UI jobs were canceled when the documentation commit triggered this replacement run. The app source is unchanged, and the focused local search/startup results above remain valid.

Artifacts: `/tmp/MFLBlitz-68.xcarchive`, `/tmp/mfl68-export/MFLBlitz.ipa`, `/tmp/mfl68-export-verification.log`, `/tmp/mfl68-export-evidence.json`, `/tmp/mfl68-upload-reauthenticated.log`.

## September 16: build 68 implementation — player-search relevance and dismissal

Player search ranks exact full names, equally weighted first/last name words, word prefixes, substrings, then conservative single-edit typos. Within each name tier it prioritizes the user's roster, other league rosters, cached current-week projections (falling back to observed points), then a deterministic name/ID fallback. Hunter Henry and Travis Hunter receive the same name-match strength; Jordan Mason is no longer pushed below every first-name Mason. Suffixes, MFL reversed-name order, NFL filters and league eligibility remain supported. Current results do not move when ownership or projections finish updating; the next query edit uses the new context. Typing adds no network request or provider usage.

The keyboard's native Done action, result-panel taps and scrolling dismiss the keyboard while retaining the query. Tapping outside closes search and consumes that tap so covered controls cannot activate. The field can be focused again, and player navigation/back retains the query and ordering.

Validation: all **22 search model tests and 10 native search journeys pass**, with zero failures (`/tmp/mfl-search-relevance.xcresult`). Coverage includes ownership/relevance tiers, typo boundaries, current-week and conflicting metrics, stable results through delayed ownership and account replacement, request-cache reuse, all five tabs, player Back, Mason after a scrolled query, both Hunter matches, keyboard dismissal, outside taps, failed ownership and largest text. Exported Hunter, Mason and accessibility-text screenshots were inspected. These changes are included in build 68; delivery is recorded above. No service change was needed.

## September 16: build 68 implementation — remove the welcome-screen flash on cold launch

The root now starts in an explicit session-restoration state before SwiftUI schedules its startup task. Onboarding is only constructed after restoration finds no account, fails without cached content, or the user chooses sign-in. Saved league content still opens directly on Scores while authentication runs; offline reads and draft/write protections are preserved. The loading view uses the system appearance instead of inheriting onboarding's forced dark appearance.

Validation: all 27 startup/reliability model checks pass, including initial render state, no saved account, expired sessions, cancel/late completion and cached/offline recovery. The native cached-startup and largest-text offline journeys pass in `/tmp/mfl-startup-fix.xcresult`. The slow-cache UI check initially missed the loading screen and then exposed a parent accessibility identifier overriding the button's identifier; the fixture timing and identifier placement are corrected. Its final run passes in `/tmp/mfl-startup-ui-final.xcresult`, verifies the loading-to-Scores transition without onboarding, and its screenshot was inspected. The preceding release revision also passed all six CI jobs. This fix was merged while collecting feedback, then included in Josh's subsequently authorized build 68 release. Build 67's binary is unchanged.

## September 16: build 67 — installed through TestFlight; league beta awaiting review

Champion Hall Owners contains 12 external tester addresses, excluding Josh; both Two Bad Neighbors co-owners are included. All received the initial iPhone/TestFlight instructions. Build 67 is now Waiting for Review for the external group, with automatic notification selected; build 66 was withdrawn from review. No public TestFlight link or staff roles were granted.

Josh requested full enhanced stats/background notifications and automatic activation through each owner's normal MFL login. The updated gateway is deployed and running on Hephaestus, with 12 allowed teams and independent device credentials for co-owners. The previously provisioned 13 manual codes were never sent and are retired. HTTPS port 10000 publishes only the scoped gateway, while private ports 8443–8445 remain intact. Authenticated HTTPS status returns 200 and production APNs signer readiness; unauthenticated, arbitrary-route and cross-league probes return 403. The temporary health-check credential was removed. A subsequent read verified one automatically enrolled device and its accepted production lineup-alert registration. App Store Connect reports Josh installed 0.7.0 (67) at 12:50 CDT. Automatic enrollment/registration are verified; visible push delivery still needs a live phone observation. The canonical Hephaestus inventory records the change. See [automatic access and operations](league-beta-access.md).

Migration ends an existing private-service Live Activity before re-registering with the new identity; alert registration reuse also checks the destination address. Build 67 also fixes notification permission recovery, moves lineup checks/alerts into a compact toolbar bell, and suppresses navigation cancellation alerts. Automatic enrollment checks membership transiently with MFL without persisting the session; the sign-in copy and privacy policy disclose this. Notification options remain opt-in and provider polling/budgets remain shared and unchanged.

Validation: 379 app tests in 40 suites pass (`/tmp/mfl67-automatic.xcresult`); normal/largest-text bell and alert-settings journeys pass (`/tmp/mfl67-final.xcresult`), and screenshots were inspected. All 14 gateway and 53 background service tests pass locally; gateway tests also pass on Hephaestus. Seventeen final access/lifecycle/migration checks also pass (`/tmp/mfl67-migration.xcresult`). Build 67 was archived from `0203592` with stable Xcode and exported successfully; strict signatures, matching app/widget versions, App Store profiles, production push entitlement and gateway settings pass (`/tmp/mfl67-export-verification.log`). Initial upload attempts stopped before transfer because Xcode had no account signed in. After Josh signed in, the same verified archive uploaded successfully at **12:42:33 CDT** (`/tmp/mfl67-upload-signedin.log`). Apple processing and encryption compliance completed. Build 67 is assigned to Owner Testing (one tester) and Champion Hall Owners (12 testers), and its external status is Waiting for Review. The superseded build 66 was withdrawn from review; beta description/review notes now distinguish build 67 automatic access from build 66 private services. A single BCC correction email was sent successfully to all 12 invited addresses, including both co-owners, with automatic-setup instructions and the Apple-review caveat. No manual service codes were sent. CI originally could not resolve its implicit latest simulator before scoring UI tests began. The CI-only follow-up pins Xcode 16.4 and creates/boots an iOS 18.5 simulator explicitly; core, backend and full app checks now pass, and three native UI jobs are still running at this checkpoint. This CI-only follow-up does not change the build 67 app binary. Phone enrollment and production lineup registration are confirmed; visible notification/Live Activity push delivery remains unverified.

## September 16: build 66 — available in owner-only TestFlight

**0.7.0 (66)** uploaded successfully at **11:20 CDT**, completed Apple processing, and was assigned to the **Owner Testing** internal group. At **11:29 CDT**, App Store Connect showed one build, one tester (Josh) and status **Invited**. Beta description, privacy/support links, feedback contact and build-specific test instructions are saved. No external testers or public invitation link were added. Phone installation and production notification delivery remain for Josh's test; the last locally verified phone install is build 65.

Apple rejected the first upload because the installed Xcode beta `27A5194q` was unsupported. Installed official stable Xcode **27A266a** alongside the beta, rebuilt the unchanged app source and uploaded the accepted archive. Archive source revision is `92b5c26e85793300097105b85bcdf0f5482ad82c` (app implementation `027aa802af5082086722ffec96f44111af38244d`); SDK build is `24A430`. The App Store export passes strict app/widget signatures, matching versions, distribution profiles, production push entitlement, endpoint and privacy checks. All six [CI jobs](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/35117838630) passed. An additional **19 checks** with stable Xcode pass, including the native retained-trade-history journey, with no failures or runtime warnings.

Josh explicitly approved creating and installing the production-only MFL Blitz APNs key. The existing Hephaestus service restarted at **11:12:27 CDT**; at **11:13:18 CDT**, HTTP 200 status reported both sandbox and production ready, zero subscriptions and no issue. Key/config permissions are owner-only. The sandbox key, owner allowlist, private routes and polling schedules are preserved; the canonical service inventory is updated. Configured readiness is not proof of push delivery.

Export-compliance source and binary inspection found Apple's URLSession HTTPS, Security/Keychain/random APIs and CryptoKit hashing, with no third-party crypto package or custom encryption implementation. The questionnaire's **None of the algorithms mentioned above** answer cleared Missing Compliance. External beta review details/access remain separate from this internal test.

Artifacts: `/tmp/MFLBlitz-66-Xcode27.xcarchive`, `/tmp/mfl66-xcode27-export/MFLBlitz.ipa`, `/tmp/mfl66-xcode27-upload.log`, `/tmp/mfl66-xcode27-evidence.json`, `/tmp/mfl66-xcode27-smoke.xcresult`. App Store Connect app ID `6812830730`, build ID `ff77c029-108f-48c8-bd2e-3b0f9ae4d29b`. [TestFlight](https://appstoreconnect.apple.com/teams/69a6de81-6983-47e3-e053-5b8c7c11a4d1/apps/6812830730/testflight) · [test checklist and limitations](testflight-beta.md).

## September 16: build 66 — signed App Store export verified

After Josh signed into Xcode, the Release archive and App Store export succeeded using automatic provisioning. The exported **0.7.0 (66)** app and widget pass strict signature verification, matching versions/identities, App Store distribution-profile checks, production `aps-environment`, and absence of development debugging entitlements. The private endpoint configuration and privacy manifest are present. The export verifier now normalizes macOS's `/var` → `/private/var` temporary-directory alias before checking IPA extraction paths.

App source revision: `027aa802af5082086722ffec96f44111af38244d`. Signed archive: `/tmp/MFLBlitz-66-Signed.xcarchive`; export: `/tmp/mfl66-testflight-export/MFLBlitz.ipa`; evidence: `/tmp/mfl66-signed-evidence.json`. [Draft PR #13](https://github.com/biggsjm/mfl-blitz-ios/pull/13) holds the candidate. Core, app and backend CI jobs passed; native UI jobs were still running at this checkpoint.

Xcode account access is verified. The available browser's App Store Connect and Apple Developer pages still require sign-in, which was requested separately. The app record/beta setup and production APNs key therefore remain pending. No upload, production key creation, tester invitation, TestFlight installation or service-role change occurred in this signing step. Build 65 remains installed on the phone.

## September 16: build 66 — TestFlight candidate implementation

Josh approved request reduction, retained trade outcomes and TestFlight preparation, with personal testing before inviting one or two people. Candidate **0.7.0 (66)** refreshes the visible tab on foreground return, shares league-week checks and refreshes waiver context once at rollover. Initial sign-in still loads the main sections. My Team uses a cached pending-offer read without all league assets; entering Trades waits for an overlapping badge read and upgrades to current assets. Every real trade retains fresh preflight and readback. The app honors both numeric and HTTP-date cooldowns; its existing host cooldown remains in-memory.

Trade History retains active offers and the latest 200 closed offers in per-league/franchise/season Keychain storage, with unread changes reflected in the badge. Failed/malformed reads do not close offers. Failed history restoration does not overwrite saved records; confirmed local responses distinguish Accepted, Declined and Withdrawn. Disappearance alone is Closed, with retained terms and an MFL link. History starts with offers the candidate observes. No authenticated background trade service or trade push notifications were added.

The existing Hephaestus scoring service is deployed with shared 60-second reads, original score fetch timestamps, 1.25-second per-host pacing, persistent HTTP 429 cooldowns and aggregate diagnostics. Public NFL schedule/injury reads use MFL's API host. Existing polling schedules and the paid NFL provider budget are unchanged. The service also selects separate sandbox/production APNs credentials and reports readiness for each environment. All **51 backend tests** passed locally and during deployment. At **10:09:53 CDT**, systemd was active, the cooldown database had mode 0600, sandbox was ready, production was not ready, and status reported no current issue. The canonical Hephaestus inventory records this deployment; the owner allowlist and private routes remain unchanged.

Validation: all **115 core tests** and **374 app tests** pass (`/tmp/mfl66-core.log`, `/tmp/mfl66-final-app.xcresult`), with no app runtime warnings. The app suite includes the two-week simulation, rollover and concurrent trade-read regressions. Three native trade journeys pass (`/tmp/mfl66-ui.xcresult`): retained terms/unknown outcome, empty inbox and large-text actions. Exported screenshots were inspected. Final storage recovery and neutral ownership labels then pass **29 focused app checks and the native history journey** (`/tmp/mfl66-trade-final.xcresult`), including the two added locked/corrupt-storage regressions. Unrelated synchronized ` 2.*` duplicates remain untouched; Xcode validation uses a clean source copy.

Release settings now opt into production push; the app privacy manifest includes the UserDefaults reason and optional-service data categories. The optimized **unsigned** archive at `/tmp/MFLBlitz-66-Unsigned.xcarchive` builds and contains matching app/widget 0.7.0 (66), private endpoint configuration and privacy declarations. It is not an installable distribution build. The signed archive attempt stopped at Xcode's **No Accounts** error and missing push-enabled provisioning profile. A distribution certificate exists; production APNs credentials are not configured, App Store Connect is signed out, and MFL client registration remains unverified. Build 65 is still on the phone. No upload or tester invitations have occurred. See the [beta instructions and remaining setup](testflight-beta.md).

## September 16: TestFlight, MFL throttling and trade outcome audit

Josh reported a roster-area rate-limit warning and a successful trade proposal whose later rejection silently disappeared from pending offers. The [readiness audit](testflight-readiness-2026-09-16.md) records confirmed request pressure, missing trade history/notifications, production push/privacy/backend-access work, and the distinction between Apple distribution requirements and product gates. Apple now accepts Xcode 27 uploads; account-specific MFL registration and App Store Connect setup remain unverified behind sign-in. The existing scoring services were inspected read-only and reported running with no current Live Activity issue. No app/service behavior, background role, build, upload or tester access was changed by this audit. Build 65 remains the installed baseline.

## September 15: build 65 — standings results and tiebreakers

Fixed the Week 1 standings failure: MFL had published league records while its global CompletedWeek remained zero. The repository now reconciles the full league’s W/L/T with a schedule horizon bounded by CurrentWeek/LiveScoringWeek and the regular season. It preserves pairwise H2H before points; future placeholder ties, inconsistent/adjusted records and incomplete schedules never manufacture places. Standings require a schedule no older than 60 seconds, including entries first cached by Calendar. Repeated reads share the existing cache. If H2H is unavailable, PCT leagues still group known winning percentages in order without assigning unverified places. The information panel links directly to MFL’s tiebreaker report.

Read-only inspection in the owner’s signed-in browser confirmed Champion Hall’s `PCT,H2H,PTS,DIVPCT` configuration, Week 1 schedule and all three division reports. Warner’s order is Bears Sausage Ditka (154.5), Two Bad Neighbors (87.0), Uber Beasts (93.0), GPT 5.0 now available (77.5); the first two teams are 1–0 and the last two are 0–1. Synthetic regressions reproduce that case without saving authenticated API responses or credentials.

Validation: all **114 core tests** pass (`/tmp/mfl65-standings-core.log`), plus **21 focused app tests** and **3 native standings journeys** on iOS 18.4 (`/tmp/mfl65-standings-tests.xcresult`). They cover preliminary results, future/horizon rejection, byes/doubleheaders, true ties, missing H2H, cache reuse, separate division/overall places, navigation and largest text. Source is tested from a copy excluding untracked duplicate files that appeared in the workspace; those local files are preserved. All **14 existing repository regressions** also pass (`/tmp/mfl65-repository-tests.xcresult`), including owner metadata, division identities, incomplete membership and repeated-read caching. The information-panel and largest-text screenshots were inspected. Signed app and widget verification passed for **0.6.4 (65)**. Installation and launch on Josh’s iPhone succeeded; installed-app readback confirmed build 65 (`/tmp/mfl65-install.json`, `/tmp/mfl65-launch.json`, `/tmp/mfl65-apps.json`). Existing private scoring service URLs and push signing were retained.

## September 14: build 64 source integration

[PR #12](https://github.com/biggsjm/mfl-blitz-ios/pull/12) collects the owner-approved work through build 64 for commit, push, and merge. The phone baseline remains build 64 described below. The merge also preserves compatibility with GitHub’s Xcode 16.4 / iOS 18.5 toolchain: PNG palette calculations use explicit typed steps, and notification authorization passes only the status enum across the callback. These changes preserve artwork and notification behavior.

Verification passes for **110 core tests**, **123 service/tool tests** (44 background scoring, 32 live NFL, 20 historical NFL, 27 provider tools), and **365 app tests** (361 Swift Testing checks plus four Live Activity render checks). All 259 local Markdown links resolve. Complete app results are in `/tmp/mfl64-merge-app-tests.xcresult`; focused logo/alert compatibility results are in `/tmp/mfl64-merge-compatibility.xcresult`.

All **79 native UI journeys** remain covered in CI, split into navigation (35), league/player/search (20), and scoring/lineup (24). Legacy assertions now check the approved compact game caption, separate owner row, complete accessible player names, equal player areas, live estimates, and My Team’s tools disclosure. Pregame cases use an NFL team that is consistently upcoming in the synthetic league. Week 1 player ownership is verified while Scores and Lineup retain Week 2. Focused iOS 18 results include `/tmp/mfl64-merge-navigation18.xcresult`, `/tmp/mfl64-merge-context18.xcresult`, `/tmp/mfl64-merge-final-context18.xcresult`, and the largest-text pass in `/tmp/mfl64-merge-game-captions-fixed18.xcresult`.

The cold Live Activity journey now opens its URL through Safari and iOS’s Open confirmation, then verifies onboarding handoff, the normal Scores navigation stack, NFL game/player details, and Back. A temporary diagnostic showed that Xcode 16’s direct application-open test API launched the app without delivering a URL to the handler on CI; all temporary app diagnostics were removed. The external handoff passes locally on iOS 18.4 (`/tmp/mfl64-cold-sheet18.xcresult`). This test is isolated in `ActivityDeepLinkUITests` so it runs early. Its readiness check uses the visible onboarding screen; Xcode’s process-state probe can remain stale after an external launch, even when all subsequent navigation and stats checks pass. Final GitHub results are recorded in the pull request.

## September 14: build 64 installed and launched — compact, uniform player rows

Following the owner’s additional request and comparison screenshot, compact mirrored player cells put actual points nearest the center position gutter, with name/NFL team, game context and abbreviated stats beside them. Full names remain in accessibility labels and player details; names shorten only when necessary. Every player in each matchup presentation shares the tallest intrinsic player height, including any injury label. Team headings occupy a separate shared row in Live players so wrapped names cannot shift the player columns. Short content leaves reserved space; larger text and long stats increase the common height without truncation. Lineups uses the same sizing behavior, including missing-player cells. The compact stat formatter changes labels only and retains every value/event, including completions, mixed yardage, TDs, turnovers and two-point conversions. Game context shortens to opponent, player-team-first score and status. The full area opens player details, including reserved space under short content. Pinned score labels fit the available width at accessibility sizes. This is presentation only and retains build 63’s ownership, simplification and league abbreviation behavior.

Early checks distinguished the visible content bounds from the reserved player area and led to making the complete area tappable. A largest-text touchdown test then tapped under the pinned header; its screenshot showed the target’s visible fragment below the header. The corrected test taps the visible intersection and explicitly checks navigation before looking for the full box score. The four equal-area/tap/TD UI journeys then passed (`/tmp/mfl64-delivery.xcresult`). The owner supplied a compact comparison-row reference during that run. All **24 native NFL stat/game-context tests pass** (`/tmp/mfl64-compact-rows.xcresult`); all **four final UI journeys pass** (`/tmp/mfl64-reference-final.xcresult`), including equal tap dimensions, row alignment, score centering, mode/detail navigation and visible receiving TDs at normal/largest text sizes. A fixed swipe-count test was replaced with visible-viewport scrolling so tall rows cannot be tapped under the pinned header. Final screenshots in `/tmp/mfl64-reference-shots/` were inspected. The final visual pass also keeps FLEX on one line in the narrow position gutter. The signed app and widget report **0.6.3 (64)** and strict signature verification passes (`/tmp/mfl64-installed-build.log`). Final installation and launch succeeded, with device readback confirming build 64 (`/tmp/mfl64-final-install.json`, `/tmp/mfl64-launch.json`, `/tmp/mfl64-apps.json`). Build 63’s 11 passing ownership/search/timeline journeys remain the verification for those unchanged flows. No provider polling/cache/budget policy, backend service, notification preference or external release changed.

## September 14: build 63 installed and launched — live players and ownership refinement

After reviewing build 62 on the phone, the owner requested simpler scoring, an active-player-only mode and visible ownership after search. **Lineups / Live players** now keeps active-game starters immediately expanded under pinned official totals. Full lineups retain completed/upcoming starters and bench. No-active-player and unknown-game states direct back to Lineups. The timeline moves to a toolbar clock. League abbreviations remain exactly as supplied, including **0005**.

Player rows retain actual points, NFL game context and all available scoring stats, including touchdowns; inline contribution arithmetic and repeated Points detail links are removed. Pregame projections appear only before kickoff. The league points breakdown and full box score remain in player details. Ownership is more prominent in results and now appears on the Week tab: the existing cached detail read was previously attached only to the Player tab. It now serves both tabs, with an immediate scoped search-ownership fallback and explicit unknown/last-known states. Search height follows direct content geometry.

The same shared NFL cache, refresh policy, provider ceiling and team subscriptions are retained. No service deployment is required. All 11 focused UI journeys pass (`/tmp/mfl63-focused.xcresult`), including dark-mode normal/largest Live players, ownership from four player entry points, timeline access, all six search journeys and receiving-TD visibility. Screenshots in `/tmp/mfl63-ui-shots/` were inspected. Signed app/widget versions and strict signature verification pass; installation, launch and device readback confirm **0.6.3 (63)** (`/tmp/mfl63-install.json`, `/tmp/mfl63-launch.json`, `/tmp/mfl63-apps.json`).

## September 14: build 62 installed and launched — Matchup mode and approved review changes

The owner approved the seven review packages plus Matchup mode. The matchup now offers **Lineups / Matchup mode**, with per-team Completed / In progress / Yet to play summaries, live starters expanded initially, exception states, and separate bench scoring. Section choices, selected week and per-mode scroll context persist while navigating; the view choice persists across launches. Official scores stay pinned after the header leaves the screen. Shared NFL game state takes precedence over a conflicting player clock; tied regulation completion stays unclassified without a verified final. Missing player totals and unexplained differences remain explicit. Newer Live Activity receipts update only the official team header, with older player rows qualified.

The other packages are implemented: adaptive readable action tint and fuller Live Activity accessible labels; common weekly player context from all four entry points; compact league point contributions and final-score hierarchy; lineup readiness with existing replacement actions and distinct acknowledged alert coverage; grouped timeline observations and verified quiet-period gap suppression; clearer Live Activity Game totals and per-team remaining labels; explicit player search with content-sized results and compact My Team tools.

Verification includes 39 initial Swift Testing checks across mode, readiness/alerts, game-day and search, followed by 35 mode/NFL-feed/receipt/refresh checks including the regulation-tie case. Four real-render Live Activity tests pass after reducing padding to fit the 160-point height constraint, including OCR and score-centering checks. All six search UI journeys pass. The same weekly player stats pass through search, matchup, lineup and roster. My Team's six destinations, lineup alert settings, aligned owner/record rows and timeline/points-detail flows pass. Normal and largest-text Matchup mode journeys pass with pinned scores and scroll restoration (`/tmp/mfl62-restoration.xcresult`); screenshots were inspected. The final game-clock conflict regression and delivery checks are recorded below when complete.

Early runs caught and corrected a List button-style interaction, inherited accessibility identifiers, Live Activity height overflow and header geometry observation. The scrolling test was also corrected to scroll beyond the full header before expecting the compact strip. Some failed Xcode beta runs stalled during diagnostic collection after reporting test results; those collectors were terminated. Passing runs produce complete result bundles.

The private background service deployed successfully and all **44 service tests pass** (`/tmp/mfl62-background-deploy.log`). No NFL-provider polling interval, daily ceiling or cache/archive policy changed; Matchup mode is derived locally, rule reads coalesce by league scope, and expanding sections makes no requests. No notification was sent, consent preference enabled, commit/push performed or external release made. Physical notification delivery, manual VoiceOver/Always On review and D/ST behavior in a real D/ST league remain unverified.

Final delivery verification passes (`/tmp/mfl62-delivery.xcresult`): 14 Matchup mode tests, four Live Activity render/OCR tests, and the normal-text mode/restoration and grouped timeline journeys. This includes the game-clock conflict regression. Final screenshots in `/tmp/mfl62-delivery-shots/` and normal/largest mode captures in `/tmp/mfl62-matchup-shots/` were visually inspected. The signed app and extension both report **0.6.3 (62)** and strict signature verification passes (`/tmp/mfl62-device-final.log`). Installation and launch succeeded (`/tmp/mfl62-install.json`, `/tmp/mfl62-launch.json`); device app readback confirms version 62 (`/tmp/mfl62-apps.json`).

## September 14: build 61 installed and launched — concise timeline and independent app review

The owner's timeline photo showed four repeated gap messages, a recording-start row and permanent technical explanations. The view now leads with score changes, collapses gaps/starts into **Recording details**, shows a single coverage note, and places the explanation/retention period behind an information button. A history with no recorded score movement has a concise empty state. Existing saved data, negative deltas, collection frequency, gap detection and server merging are unchanged.

Eight native game-day checks and two normal/largest-text timeline → recording details → player points-breakdown journeys pass (`/tmp/mfl61-timeline-verified.xcresult`). The first UI run expected tracking history from a preview that has no scoring receipt; the corrected run uses an explicit DEBUG-only five-event preview matching the supplied screenshot, without changing saved archives, accounts or feeds. It verifies that all four gap rows are hidden initially and available on expansion. Screenshots in `/tmp/mfl61-timeline-shots/` were visually inspected. The signed final device build and strict signatures pass; app/widget report **0.6.3 (61)** (`/tmp/mfl61-device-final.log`). Installation and launch succeeded, and device app readback confirms build 61 (`/tmp/mfl61-install.json`, `/tmp/mfl61-launch.json`, `/tmp/mfl61-apps.json`).

At the owner's request, three AI reviewers independently assessed football/fantasy usability, sports app design and Apple design quality, then read each other's reports and discussed priorities directly. All three explicitly accepted the [seven-package prioritized plan](reviews/2026-09-14-priorities.md). The independent reports record evidence, disagreements resolved and remaining runtime validation; no real-world credentials or award certification are claimed. Source coverage spans all five tabs; visual coverage combines the user photo, recent search and older cross-checked scoring/alert/Live Activity captures. These are review proposals, not seven implemented changes. No server deployment, paid-data request-policy change, notification send, commit, push or TestFlight release.

## September 14: build 60 installed and launched — inline global search

Search now follows FeedCast's inline-field interaction: the magnifying glass expands a focused field under the current toolbar and a bounded results panel over the existing page. There is no search sheet or separate search stack. All five tabs and matchup detail share the interaction; opening a player uses the originating navigation stack, and Back restores the query without reopening the keyboard. Close clears the query while retaining the underlying tab, matchup and draft state. Search still filters the shared local catalog and adds no typing-triggered requests or NFL polling.

Build **0.6.3 (60)** includes the lineup-alert crash fix from build 59. **19 native checks** pass (15 search and four complete alert-controller tests) in `/tmp/mfl60-search.xcresult`. All six distinct search UI journeys pass across that result and `/tmp/mfl60-navigation-verified.xcresult`: five-tab entry with player Back, matchup return, slow ownership, offline ownership, league/team filtering and largest accessibility text. The initial matchup test exposed an inherited accessibility identifier, corrected before the passing navigation run. Results-panel sizing was refined after screenshot review; largest-text and navigation checks pass again in `/tmp/mfl60-layout-final.xcresult`. The final title-mode change passes the five-tab journey including player Back from both Scores and Board (`/tmp/mfl60-title-final.xcresult`). Normal and largest-text screenshots were visually reviewed; final Board and Scores captures retain the page title and complete owner/status line. Manual VoiceOver and iPad checks remain separate release checks.

The final signed device build and strict signatures pass (`/tmp/mfl60-device-final.log`); app and extension both report **0.6.3 (60)**. Installation and launch succeeded, and the phone's app readback confirms build 60 (`/tmp/mfl60-install.json`, `/tmp/mfl60-launch.json`, `/tmp/mfl60-apps.json`). Physical lineup-alert connection/delivery confirmation is still pending the owner's Settings check. No server deployment, paid-feed policy change, commit, push or TestFlight release.

## September 14: build 59 installed and launched — lineup alert crash fix

Both phone reports (`MFLBlitz-2026-09-14-125344.ips` and `MFLBlitz-2026-09-14-125358.ips`) show Swift's exclusivity trap at `LineupAlerts.swift:156`. The optional-chain assignment opened a write to the observable `saved` value while its right side read `saved.revision` again. Enabling alerts saved the preference before reaching the trap, so reopening could crash at the same registration step.

Build **0.6.3 (59)** computes the next revision before modifying saved state, guards counter overflow, and preserves existing preferences and registrations. Notification access and transport are injectable for deterministic tests of the complete enable path. Four new controller tests cover permission/token sequencing, enabling and relaunch, a failed registration followed by relaunch/retry, permission denial and opt-out cleanup. Those four plus eight game-day checks pass (`/tmp/mfl59-alerts-verified.xcresult`). The initial test harness build had compile errors, corrected before the passing run; it was not an app test failure.

Signed device build and strict signature check passed (`/tmp/mfl59-device.log`); installation and launch succeeded (`/tmp/mfl59-install.json`, `/tmp/mfl59-launch.json`). Josh was asked for the Lineup alerts connection status to confirm the physical opt-in flow; notification delivery remains distinct from registration. No server code or paid NFL request policy changed. Inline search requested during this investigation proceeds separately.

## September 14: build 58 installed and launched — reviewed player stat translations

Josh reported Cam Skattebo's missing stat line and requested a maintainable translation table. The cached NFL box and roster call him **Cameron Skattebo (31107)**; MFL calls him **Cam Skattebo (17045)**. Exact-name matching rejected a real box containing 18 carries, 81 rushing yards and a touchdown. Build 58 attaches that box through reviewed IDs, and the league breakdown explains **8 + 6 = 14 points**.

The shared NFL service now owns one versioned table with 14 observed inconsistencies. Each entry includes both IDs/names, season, team/position and review evidence. The service checks source identity before supplying optional translation fields; the app and background Live Activity require matching MFL identity and reject ambiguity. Future table additions only need service deployment. Provider names, box values, freshness receipts and polling/budget behavior remain unchanged. A repeatable offline audit reports unresolved players and review suggestions without making provider calls or automatically accepting similar names. See [table, audit results and maintenance instructions](player-stat-translations.md).

Audit evidence: public 2026 MFL catalog, league 41333 Week 1 roster/scoring exports and a read-only copy of Hephaestus's saved NFL boxes/rosters. Across 220 roster players: 171 exact matches, 14 reviewed translations, 14 upcoming, and 21 without a matching stat record or nonzero listed MFL points. All 172 nonzero scorers match after the fix (`/tmp/mfl58-audit-fixed.json`). This verifies identity coverage of those cached records, not complete provider coverage or future live-game behavior.

**99 focused checks pass**: 24 native matching/cache/points tests (`/tmp/mfl58-native.xcresult`), 32 NFL cache tests (`/tmp/mfl58-nfl-tests.log`), and 43 background scoring tests (`/tmp/mfl58-background-tests.log`). Regressions cover Cam's decoded translation/TD breakdown, conflicting/missing IDs, invalid tables, ambiguous candidates and source metadata disagreement. Existing polling, quota, final-correction, lifecycle, timeline and opt-in-alert tests pass. The existing presentation is unchanged, so no new screenshot/UI journey was added for this lookup fix.

Both services were deployed with their passing remote suites (`/tmp/mfl58-nfl-deploy.log`, `/tmp/mfl58-background-deploy.log`). All 14 translations were verified against deployed cached records without paid fetches. Final health: both services active; NFL 64/6,000 requests, no pause/throttle and no viewing demand; background pushReady true, no subscriptions or issue. The first direct loopback background status read correctly refused a request without its owner header; the authenticated follow-up passed. No alert consent was enabled.

Signed build and strict signatures pass; app/widget both report **0.6.3 (58)** (`/tmp/mfl58-device.log`). Installation and launch succeeded; device app readback confirms 58 (`/tmp/mfl58-install.json`, `/tmp/mfl58-launch.json`, `/tmp/mfl58-apps.json`). No new subscription purchase, recurring audit, commit, push or TestFlight release.

## September 14: build 57 installed and launched — game-day enhancements

Josh approved Live Activity updates, league-specific points breakdown, opt-in lineup notifications (including a separate incomplete-lineup option), persistent matchup history and D/ST support. The implementation is complete; see [feature behavior, refresh budgets and limitations](game-day-enhancements.md).

Live Activities retain their colors/logos and live estimates, add per-team playing/yet-to-play counts, next kickoff while waiting, and a current verified stat line when one starter explains the last observed MFL change. An explicit Continue action supports long game days without silently restarting user-dismissed activities. Native renders with real encoded artwork, short/uneven/large scores, long names, stale state, waiting and continuation fit the 160-point compact height. The actual eight-hour ActivityKit lifetime remains.

Points breakdown reads the actual league rules and descriptions through MFL's cached API. Strange's source-shaped test explains **2 reception points + 1 receiving-yardage point + 6 touchdown points = 9**; Dak's completion/yardage/TD/interception example explains 25. Unsupported threshold/distance rules and missing stats remain visible rather than guessed. MFL points and any unexplained difference remain separate. Large-text labels and values stack vertically.

Lineup alerts are independently off by default: pre-kickoff reminder, unavailable starter, and incomplete lineup. The private service verifies saved starters before kickoff, not local drafts, uses recent explicit MFL Out/IR/Inactive reports, deduplicates deliveries and prevents opted-out registrations from being resurrected. Pending removal survives offline periods; token changes and manual retry can replace obsolete registrations. Registration lasts up to eight days and renews when the current lineup is opened. There are no paid NFL requests for these alerts. No real notification consent or device enrollment was enabled during testing; live notification receipt awaits the owner's opt-in and a qualifying kickoff.

Timeline state survives relaunch, stores negative changes and source gaps, and merges continuous background history instead of duplicating it with an aggregate reopening delta. Both app and server histories are bounded and expire after 21 days. Background observations exist only while matchup subscriptions are checked. D/ST uses explicit team totals and independent receipts; no individual-defender aggregation or substitution of MFL OPA/STPA for scoreboard points allowed. The current league has no D/ST, so this path is fixture-tested.

Validation: **19 MFL client tests, 43 distinct native tests, three distinct UI journeys, 29 NFL-service tests and 42 background-service tests** pass across final runs. Evidence: `/tmp/mfl-57-core-final.log`, `/tmp/mfl-57-final.xcresult`, `/tmp/mfl-57-timeline-final.xcresult`, `/tmp/mfl-57-ui1.xcresult` (normal walkthrough and opt-in screen pass; the initial large-text timeout was superseded by the passing `/tmp/mfl-57-accessibility-final.xcresult`), `/tmp/mfl-57-nfl4.log`, `/tmp/mfl-57-background5.log`. Screenshot exports in `/tmp/mfl-57-layout-shots/`, `/tmp/mfl-57-ui-shots/` and `/tmp/mfl-57-accessibility-shots/` were visually reviewed. No simulator tests opted into real notifications or changed MFL data.

Both private services are deployed and their remote tests pass (`/tmp/mfl-57-nfl-deploy.log`, `/tmp/mfl-57-background-deploy-final.log`). All three existing private services are active. Health readback shows **50 of 6,000** paid requests used, no slowdown/pause, no active viewing demand, and **zero** lineup-notification registrations. The signed device build succeeds, with strict signing verification and app/extension version **0.6.3 (57)**. The final install succeeded after one transient wireless disconnect; device readback confirms **0.6.3 (57)** and launch succeeded. Evidence: `/tmp/mfl-57-device-delivery.log`, `/tmp/mfl-57-install-retry.json`, `/tmp/mfl-57-apps.json`, `/tmp/mfl-57-launch.json`. App data is retained. No commit, GitHub push/merge or TestFlight release.


## September 14: build 56 installed — visible scoring events and archived final stats

The paid feed already contained Brenton Strange's receiving touchdown, but the compact matchup formatter omitted it. The production decoder now confirms **2 rec · 23 rec yd · 1 rec TD** from the actual cached Week 1 response. Summaries include receiving/rushing/passing TDs, interceptions thrown, two-point conversions, fumbles lost/recovered, kicking conversions and available distance buckets, individual defensive events and return scores. Duplicate return/defensive TD fields use one source. No two-group or three-line cap can hide later events; the existing full player box presentation remains.

Josh agreed to keep slower final-stat/correction checks rather than freeze the last live box at the final whistle. Fast player polling remains limited to active NFL game states. Final boxes refresh every five minutes through six hours after kickoff, hourly through two days, daily through seven days, then receive one last correction read on the next requested visit and remain cached. An uncached historical box is fetched once. Archived final receipts do not expire solely with age, and known matching roster identities remain usable for final games. Historical viewing does not accelerate schedule checks for unrelated live weeks. Phone reads still share one server worker/cache, with no provider-cache bypass. All provider work requires viewing demand and obeys the existing 6,000/day budget and backoff. See [the exact refresh policy and limitations](live-nfl-scoring.md).

Validation: **25 NFL-service tests**, **14 native NFL formatting/identity/cache tests**, and **three distinct scoring UI journeys** pass. Coverage includes the actual Strange field shape, multi-group TD/turnover retention, null versus zero, composite kicking stats, duplicate return fields, final cache across restart, cold historical fill, inactive statuses, kickoff discovery, request budget/backoff, and visible receiving TDs at normal and largest accessibility text sizes. Evidence: `/tmp/mfl-score56-server-final-tests.log`, `/tmp/mfl-score56-tests.xcresult`, `/tmp/mfl-score56-final.xcresult`. Screenshots in `/tmp/mfl-score56-shots/` and `/tmp/mfl-score56-final-shots/` were visually checked. The exact production Swift decoding/validation/matching definitions accepted the deployed 16-game feed and asserted Strange's complete line (`/tmp/mfl-score56-contract.log`). Actual live-game latency remains unverified in this completed-game session; independent MFL/NFL feeds can briefly disagree, and unavailable provider fields or league-specific bonuses can still prevent exact point reconstruction.

The updated NFL service is deployed; all three private NFL/background services report active. Request-budget health showed **49 of 6,000** used, without pause or slowdown (`/tmp/mfl-score56-deploy.log`, `/tmp/mfl-score56-health.log`, `/tmp/mfl-score56-status.json`). The device build and strict signing verification pass. Installation succeeded and device readback confirms **0.6.3 (56)** (`/tmp/mfl-score56-device.log`, `/tmp/mfl-score56-install.json`, `/tmp/mfl-score56-apps.json`). Automatic launch was denied because the phone is locked (`/tmp/mfl-score56-launch.log`); open Blitz after unlocking. Existing data is retained. No commit, GitHub push/merge or TestFlight release.

## September 14: build 55 installed and launched — shared NFL stats and faster refresh

Build **0.6.3 (55)** connects the paid NFL feed to matchup rows and the player's Week screen. NFL game scores, actual quarter/clock, halftime/overtime/final states, compact player stat lines and grouped box-score fields refresh independently of official MFL fantasy points. Unique full-name, NFL-team and position matching is required; ambiguous or absent data stays unavailable. Source receipts remain distinct and older data remains visible during errors.

The private `mfl-nfl-live.service` is deployed on Hephaestus, with one SQLite cache/worker shared by all phones. It reads live game status every 30 seconds and player boxes every 60 seconds when requested, prioritizes viewed teams, coalesces reads, reduces completed-game checks and stops idle work after demand expires. Usage survives failures/restarts, slows at 5,000/5,500/5,800 calls and stops at **6,000 per UTC day**, with additional provider-header and Retry-After protections. The existing background MFL service is deployed with a 60-second interval. See the [refresh design and operating limits](live-nfl-scoring.md).

Foreground MFL scoring now refreshes on score-page entry and approximately each minute during live play, with longer intervals between games, in historical weeks and after failure. Manual refresh joins an ongoing score request. Independent NFL work cannot block the fantasy score update. Existing Live Activity navigation, artwork, estimates and final-confirmation rules remain.

Validation: **19 NFL-service tests**, **33 background-service tests**, **52 distinct native scoring/refresh/identity tests**, **41 MFLCore client/player-tools/refresh tests**, and **8 scoring UI journeys** pass across the recorded runs. The initial parallel Xcode run stalled after its UI suite; the affected native suites pass with serial execution and time limits. The new UI test initially expected separate accessibility labels inside LabeledContent; it was corrected to inspect the actual grouped labels, and passes in both isolated and final runs. Screenshots from `/tmp/mfl-score55-shots/` were visually inspected. Native evidence: `/tmp/mfl-score55-isolated.xcresult`, `/tmp/mfl-score55-refresh.xcresult`, `/tmp/mfl-score55-final.xcresult`; seven existing UI journeys passed in the initial run's `/tmp/mfl-score55-tests.log`. Server/core evidence: `/tmp/mfl-score55-server-tests.log`, `/tmp/mfl-score55-background-tests.log`, `/tmp/mfl-score55-core-tests.log`.

The deployed service returned 16 Week 1 games and 971 game-player entries. The app's exact production decoding/validation/matching definitions accepted this real response; Dak's line resolves to 175 passing yards, 2 passing TDs and 14 rushing yards. The service reported **47 of 6,000** requests used after initial warming, with no pause or slowdown. These are provider observations, not an independent accuracy comparison. No game was live during verification; actual provider game-day latency and physical-phone game-day acceptance remain to be observed. Aggregate fantasy D/ST boxes and postseason enrichment are not included in the player-stat endpoint.

The signed device build succeeds (`/tmp/mfl-score55-device.log`). App/widget versions, strict signatures and both private service URLs verify. Installation and launch succeeded (`/tmp/mfl-score55-install.json`, `/tmp/mfl-score55-launch.json`), and device readback confirms **0.6.3 (55)** (`/tmp/mfl-score55-apps.json`). Existing app data is retained. No commit, GitHub push/merge or TestFlight release.

## September 14: API-NFL Pro active; current-season box score verified

Josh authorized the $15/month API-NFL plan for seasonal use. Direct-dashboard checkout completed for one prepaid month, $15 total including tax, with no automatic renewal. The account and Hephaestus's existing key both report Pro, 7,500 requests/day, active until **October 14, 2026 at 10:21 AM Central**. No future purchase, reminder or automatic renewal was created.

Hephaestus successfully read 2026 coverage and the Dallas–Giants Week 1 final: 67 players, including Dak Prescott's passing and rushing breakdown. The current schedule also exposed seven unresolved postseason fixtures that the historical normalizer rejects; this needs explicit treatment in the current-season adapter. These reads verify paid access and completed-game response shape, not in-game latency or independent statistical accuracy. See [provider and billing evidence](nfl-stats-service.md#api-nfl-pro-activated-september-14-2026).

No native or deployed service code changed for this subscription step. Build 54 remains on the phone; normal scoring enrichment, player-ID mapping and live-game validation remain pending.

## September 14: build 54 installed and launched — remove inline clock explanation

Josh confirmed physical NFL game context is visible in build 53 and requested removal of the long quarter/clock explanation from the player Week card. Build **0.6.3 (54)** removes that inline paragraph. Approximate-clock notation and the existing freshness information popover remain; no scoring, game-state, provider or background-service logic changed.

The signed device build passes (`/tmp/mfl-score54-device.log`), app/widget versions and strict signatures verify, and installation/launch succeeded (`/tmp/mfl-score54-install.json`, `/tmp/mfl-score54-launch.json`). Device app readback confirms version 54 (`/tmp/mfl-score54-apps.json`). No new tests were added for this text removal; the build-53 behavior checks still describe the unchanged logic.

Read-only Hephaestus health check succeeded: the NFL test service has its key configured, permits 2022–2024, and reported zero provider requests used against its 20/day service budget. Current live-stat options were checked against primary documentation; see [NFL service options](nfl-stats-service.md#current-live-stat-options-september-14-2026). No paid plan, trial signup, new provider integration or service change was authorized or performed.


## September 14: build 53 installed — direct score navigation and NFL game context

Weekend feedback identified a modal Live Activity destination, stale in-app totals on entry, and missing game/player context. Matchup links now select Scores and push into its normal navigation stack. Existing lineups and their selected week remain unchanged; a link for another week owns its scoring snapshot. The exact same link is handled on cold launch after reconnecting.

The hero can immediately use a newer, scope/week/team-matched ActivityKit score and live estimate. It never advances the receipt attached to the older player rows. Cold starts can display the matching activity's team identities and totals before lineups arrive. Entry requests a fresh MFL read, waits for an already-running startup read instead of silently dropping the request, and accepts later score corrections. Existing artwork/background registration remains unchanged.

NFL game context now uses MFL's weekly `nflSchedule` score, possession, red-zone and game-time fields. The independent read retains its actual receipt and uses a 90-second freshness limit, including when roster research previously cached the same endpoint for six hours. One shared weekly read serves the matchup and player pages without holding up fantasy scores; failures retain and label older NFL scores. Scope/week validation, cancellation and session resets prevent cross-league display. ~Q is an **approximate regulation quarter/clock** calculated from game seconds remaining. Exact quarter boundaries display remaining game time; halftime/overtime are not invented, and a tied score at zero is labeled regulation complete.

Provided player stat lines are visible at ordinary text sizes as well as on the player's Week page. **Full live box scores remain unresolved:** the September 14 read of the actual Week 1 `liveScoring&DETAILS=1` feed returned empty `updatedStats` for every player. The existing API-NFL service is restricted to approved 2022–2024 testing; it has not been upgraded or connected to 2026 scoring. No subscription was purchased. A paid-feed preference was requested; the independent MFL improvements proceed without one.

Validation: 64 focused native checks passed in `/tmp/mfl-score53-verified.xcresult`; eight final receipt/refresh checks (including three repeats) passed in `/tmp/mfl-score53-final.xcresult`, for **69 distinct app checks**. All **seven scoring UI journeys** pass across those results after correcting the URL test setup: Xcode restarts the app when opening a URL, so Preview must be resumed before the pending link is consumed. The final result verifies cold URL → normal Scores matchup → NFL game/player detail → Back, plus existing owner/record alignment and Week/Player switching. **10 MFLCore player-tools tests pass** in `/tmp/mfl-score53-core-clean.log`; the first package run hit stale resource-fork signing metadata, resolved with an isolated temporary build directory. Native screenshots were inspected in `/tmp/mfl-score53-screens/` and `/tmp/mfl-score53-final-screens/`. These are synthetic layout/data checks, not a full live-game or player-stat-provider validation.

Signed device build succeeds (`/tmp/mfl-score53-device.log`); app/widget version 53, strict signatures, development push entitlement and the existing private sync URL verify. Build **0.6.3 (53)** installed on Josh’s iPhone (`/tmp/mfl-score53-install.json`), and the device app list confirms version 53 (`/tmp/mfl-score53-apps.json`). Automatic launch was refused because the phone is locked (`/tmp/mfl-score53-launch.json`); Josh must unlock and open Blitz. Physical game-day acceptance is pending. An extra manual simulator warm-link attempt was unavailable because Xcode had shut down the simulator after tests; it does not add validation beyond the passing native URL journey. No server changes, commit, GitHub push/merge or TestFlight release.



## September 13: build 52 installed — prepared artwork reaches background scoring

Josh confirmed the red-to-blue reset still occurred on build 51. The exact remaining handoff problem was identified: after publishing prepared artwork locally, Blitz re-read `Activity.content.state` and registered that observable snapshot, which still lacked the newly prepared artwork. Build 51's server preservation only helped after artwork had reached the server; its phone readback confirmed both logos were absent. A newer push could also replace a locally prepared state with an artwork-free snapshot.

Build **0.6.3 (52)** sends the exact prepared content to ActivityKit and background registration, stores it before asynchronous publication/token arrival, merges newer score receipts while retaining the prepared art, and queues artwork completed during another update. It also clears submitted/final-confirmation state at the actual end boundary. Scope and generation guards prevent an old artwork publication from reaching a replacement activity.

**35 focused native checks pass** (`/tmp/mfl-activity52-verified.xcresult`): 11 background transport tests, 13 activity presentation tests, eight lifecycle tests and three native layout tests. New regressions inspect the actual streamed URLSession PUT body with a deliberately lagging ActivityKit readback, preserve newer scores during publication, and reject cross-activity publication. The first test run exposed a test-harness assumption that URLSession retains `httpBody`; capturing its `httpBodyStream` corrected the harness, and all checks then passed. Device build, app/widget version 52, signatures and development push entitlement verify (`/tmp/mfl-activity52-device.log`).

Build 52 installed and launched on Josh’s iPhone; device readback confirms **0.6.3 (52)** (`/tmp/mfl-activity52-install.json`, `/tmp/mfl-activity52-launch.json`, `/tmp/mfl-activity52-apps.json`). The server now receives **both real logo thumbnails and the red/orange palettes**, unlike build 51. Two subsequent scheduled background updates were verified in `/tmp/mfl-activity52-background-observation.log` (100 and 180 seconds after observation began): each was accepted after the latest phone registration, both logos survived the APNs budget calculation, and their image hashes and red/orange RGB values matched the starting artwork. This used sanitized database metadata and the production payload encoder, without extra MFL polls or pushes. Owner follow-up: Josh reports “Seems to be working” after build 52. This is a positive physical-phone check for the artwork fix; longer-term stability and the earlier disappearance’s underlying cause are not established by this brief observation. No server redeployment was needed for this fix. The unrelated disappearance's underlying iOS cause remains unconfirmed. No commit, GitHub push/merge or TestFlight distribution.

## September 13: build 51 installed and launched; service deployed

Josh reported that the Live Activity disappeared again after build 50 and changed from team colors to blue after reopening Blitz. A confirmed artwork bug was found: adoption reset the native artwork fields to nil, then registration overwrote the server’s saved artwork. Sanitized service readback showed both artwork objects absent while a live subscription continued receiving accepted pushes. The repeated disappearance itself remains unconfirmed; the earlier visible return did not establish sustained stability.

Build **0.6.3 (51)** preserves artwork across adoption/failed downloads and re-registration, matches the same teams across provider ID-order changes, replaces Live Activity records with **Live est.** values (Lock Screen and expanded Dynamic Island), and sends starter projection inputs for background recalculation. In-app owner/record alignment and records remain. Both paths require two fresh complete final reads before automatically ending. The phone saves a bounded lifecycle history visible in Background scoring; the server records sanitized end/deletion/expiry reasons.

**33 server tests and 44 native tests pass.** Native validation includes encoded artwork recovery, projection registration/availability, final confirmation, persistent diagnostics and actual native rendering at 343/408 points. Cards were visually reviewed, score alignment and rendered estimates are checked, and all layouts fit 160 points. Evidence: `/tmp/mfl-activity51-tests.xcresult`, `/tmp/mfl-activity51-server-tests.log`, `/tmp/mfl-activity51-shots/`. The final refresh guard also passes all 16 lifecycle/registration checks (`/tmp/mfl-activity51-final-lifecycle.xcresult`). Signed device build passes at `/tmp/mfl-activity51-device.log`; app/widget version 51, signatures, development push entitlement and private service URL verify.

The service was deployed and all 33 checks passed on Hephaestus (`/tmp/mfl-activity51-deploy.log`). The first installation attempt could not reach the iPhone. After Josh returned home and requested installation, build **0.6.3 (51)** installed and launched successfully. Device readback confirms bundle version 51 (`/tmp/mfl-activity51-reconnect-install.json`, `/tmp/mfl-activity51-reconnect-launch.json`, `/tmp/mfl-activity51-reconnect-apps.json`). The service received a new activity registration with starter projection inputs and both team estimates. A follow-up read confirmed an accepted background push and both live estimates; the subscription still lacked logo thumbnails at that check, so physical artwork delivery is not verified. No build-51 physical Lock Screen or closed-app stability confirmation is claimed. No commit, GitHub push/merge or TestFlight distribution.


Updated September 13, 2026. [Roadmap](roadmap.md) is the remaining execution plan; [changelog](../CHANGELOG.md) records private-build history.

## September 13: installed build 50 — Live Activity retention and recovery

**Owner confirmation:** Josh confirmed the Live Activity is back on the Lock Screen after build 50 was installed and launched. This confirms visible recovery; longer game-day stability remains a separate observation.

Josh reported that the check time advanced and then the Live Activity disappeared. At inspection, Hephaestus still had the original subscription, an unexpired lease, live phase/seven playing starters, and successful APNs updates; there were zero deletion tombstones. No MFL Blitz crash report or persisted dismissal was found. This rules out a server-issued final/end or lease expiry at that point, but does not establish the precise phone-side removal cause. Normal reopening of build 49 then created a fresh activity subscription and retired the old subscription. The owner’s advancing-check observation confirms a visible background update, not sustained stability.

A concrete premature-end path was corrected: the controller formerly ended every activity when a refresh failed the strict start-eligibility check. It now distinguishes creation from retention. Missing owner totals/matchup or uncertain clocks preserve current content; an existing activity waits between games. Confirmed completion requires known starter clocks and consistent remaining counts and leaves the final score visible for 15 minutes. Initial creation still needs an actively playing starter. Settings → Game day now shows observed activity status and offers **Restart Live Activity**, which explicitly clears the saved dismissal and refreshes current-week scores.

All **27 focused native tests pass** (`/tmp/mfl-activity50-lifecycle.xcresult`): four retention/lifecycle cases, seven background transport/cleanup cases and sixteen existing league-extra safety checks. Signed Debug **0.6.3 (50)** built, installed and launched normally on Josh’s iPhone; device readback confirms 50, both signatures verify, and the app retains its development push entitlement and private service configuration. App data was preserved. Build log: `/tmp/mfl-activity50-device.log`. The server Unicode hotfix below remains deployed. The owner confirmed visible recovery after this update; longer game-day stability remains under observation, and the original removal cause was not reproduced. No commit, GitHub push/merge or TestFlight distribution.

## September 13: background push punctuation hotfix

The owner’s Lock Screen screenshot exposed records rendered as `0u20130` instead of `0–0`. A real curl-to-loopback regression reproduced the corruption: the outer curl configuration used JSON’s default ASCII Unicode escapes, but curl does not interpret `\uNNNN` sequences. The stored records and original ActivityKit JSON were correct; corruption occurred in the final HTTP transport. This also affected Unicode minus signs, middle dots, accented names and emoji.

The sender now preserves UTF-8 while retaining the escaping needed for quotes/backslashes and the stdin-only handling of tokens/JWT/payloads. The new regression compares the received HTTP body byte-for-byte, including records, negative scores, accented names, quotes, emoji and JSON control escapes. It failed before the fix, then all **25 server tests passed locally and on Hephaestus** (`/tmp/mfl-push-unicode-fixed.log`, `/tmp/mfl-push-unicode-deploy.log`). The patched service is deployed, with its existing subscription and private key preserved. No native change or replacement phone build is required; build 49 remains installed.

The screenshot is evidence that the sender’s content reached the Live Activity, but it does not establish sustained locked-phone freshness. Corrected visible records and advancing check times remain awaiting owner observation. After deployment, the service reports one retained subscription, one accepted Apple push and no issue. Acceptance confirms the corrected payload reached APNs; visible correction remains unconfirmed.

## September 13: installed build 49 — background scoring, live estimates and aligned records

Push-enabled Debug **0.6.3 (48)** is installed and launched on Josh’s iPhone. Device readback confirms build 48; both app and widget signatures and matching versions verify. Only the app has `aps-environment: development`. The existing app data is preserved.

After Josh’s explicit approval, `com.biggsjm.MFLBlitz` was registered with Push Notifications, **MFL Blitz Push Development** was generated for the existing development certificate and Josh’s iPhone, and **MFL Blitz Background Scoring** was created as an APNs-only, topic-specific **Sandbox** key. The key is stored and validated only on Hephaestus with mode 600; the downloaded local copy was removed. The installed profile expires September 13, 2027. The build script uses manual app signing and existing widget automatic signing, so Xcode account sign-in is unnecessary.

The owner reported **Connected**. The private status route reports `pushReady: true`, one real subscription, eight accepted Apple pushes and no issue. Hephaestus’s user service is active with lingering enabled. **Locked-phone visible delivery is awaiting the owner’s observation**; Apple HTTP 200 is acceptance, not proof of display. The historical NFL service and its private route remain independent.

The requested scoreboard follow-up **0.6.3 (49)** is installed; device readback confirms version 49 and both signatures/versions verify. Automatic launch was blocked by the locked phone; open Blitz once after unlocking. Its existing app data and private background configuration are preserved. It removes the leading/trailing margin line and places owner and record text in shared rows below the team names. Team score blocks use a labeled **Live est.** once play begins: official points already scored plus the remaining-game-time fraction of each starter’s pregame projection. It assumes an even scoring rate, not a provider live forecast; it does not model injuries, possession or overtime. Unknown/ambiguous/stale inputs suppress the estimate; final matchups hide projections. Player rows retain pregame projections; the Live Activity retains actual scores.

All **13 scoring-presentation tests and one native matchup UI test pass** (`/tmp/mfl-background49-projections.xcresult`), covering progression, official corrections, completed/upcoming players, bench exclusion, missing data and freshness, plus matching owner/record row positions with wrapped team names. The exported native screenshot was inspected. Signed push-enabled build 49 succeeds (`/tmp/mfl-background49-device.log`); installation and device readback succeeded, with launch blocked only by the device lock. The earlier background implementation passed 24 server and 36 native checks; see the build-47 receipt below. Source remains in the existing dirty checkout; no commit, GitHub push/merge or TestFlight distribution is claimed.

## September 13: build 47 background scoring implementation (before Apple activation)

Signed Debug **0.6.3 (47)** is installed on Josh's development iPhone; device readback confirms version 47. Automatic launch was blocked by the locked phone; open Blitz after unlocking. App/extension versions match and signatures verify. The existing app container was preserved. This normal build retains foreground scoring and shows **Apple push setup required** under Settings → Background scoring; installation alone does not enable closed-app delivery.

The independent `mfl-live-activity.service` is enabled and running on Hephaestus behind Tailscale-only HTTPS 8444 → loopback 8792. The existing historical NFL service/8443 route remains active. Public Champion Hall Week 1 live scores were verified readable from Hephaestus without MFL credentials. Config and subscription database permissions are 600, unidentified backend requests return 403, and private HTTPS status reports `pushReady: false`, zero subscriptions and zero accepted pushes. No score polling occurs without a configured APNs key.

Implemented: 90-second coalesced MFL polling, official-total score changes with conservative starter attribution, APNs payload size/date handling, token rotation and request ordering, bounded subscription lifetime, failure backoff, durable device cleanup and server deletion tombstones, optional final/between-games presentation, private owner access, and a push-capable build script. See [background scoring and activation](background-scoring.md).

Validation: **24 synthetic server checks pass on Hephaestus**, including real local HTTP access-control checks. **36 native checks pass** with `MFL_BACKGROUND_PUSH` compiled: seven sync transport/cleanup/date tests, ten activity presentation/artwork tests, three native layout checks and sixteen existing league-extra safety tests (`/tmp/mfl-background47-verified.xcresult`). The final static/dynamic payload-budget addition also passes all seven sync checks (`/tmp/mfl-background47-budget.xcresult`) and the normal signed device build. It accounts for attributes as well as APNs wrapper bytes while retaining normal logos. These checks do not demonstrate actual Apple delivery.

At build-47 delivery, Apple activation was pending: Xcode was signed out and the cached wildcard profile lacked push. Automatic approval review required explicit authorization for the new App ID/profile/key. Josh subsequently approved those specific changes; the current activation receipt above supersedes that blocker. The privacy policy and service plan document the authorized data flow; MFL login/drafts remain device-only.

## Previous installed private build — 0.6.3 (46), Live Activity alignment and color

Installed and launched signed Debug **0.6.3 (46)** on Josh's development iPhone September 13; device readback confirms version 46, and app/extension versions and signatures verify. Each score is centered over its team name with logos in the header corners. The background uses one saturated dominant hue per logo and a soft transition, without the photo watermark or dark overlays that muted build 45. Numeric fallback IDs such as `0005` render as team-name initials.

Artwork now prefers an adaptive 16-color indexed PNG within the existing 900-byte per-logo cap. URL-safe Base64 avoids JSON slash expansion that can trigger the payload fallback; old artwork payloads still decode. Native previews use real encoded/decoded fixture logos rather than injected images, and confirm that both images survive the complete activity size guard and serialization.

All **10 model/artwork and three native layout tests passed** on iPhone 17 Pro Max / iOS 27 (`/tmp/mfl-activity46-final.xcresult`). Ten renders cover 343/408-point widths, light/dark environments, long/negative/three-decimal scores, stale content, and equal/uneven score pairs. Direct score-glyph bounds are compared with recognized team-name centers, including `0.0`; complete three-decimal score text is also recognized. All cards fit the 160-point height. Final screenshots were reviewed. Earlier test bundles retain an OCR failure where Vision omitted a visibly rendered large zero; the final test measures score glyphs directly instead. These are focused native renders and model checks, not a full app/UI or physical Lock Screen audit. Actual team-logo rendering and the new colors on the phone await owner observation. Background push scoring remains pending.

## Previous installed private build — 0.6.3 (45), live matchup presentation

Installed and launched signed Debug **0.6.3 (45)** on Josh's development iPhone September 13. Device readback confirms bundle version 45; the app and embedded Live Activity versions match and their signatures verify. Installation replaced the existing app without deleting its container. No GitHub push/merge, TestFlight distribution or live league mutation is claimed.

The Live Activity now has `MFL Blitz - Week N` at the top, outer team logos, a blend of logo-derived colors and faded artwork, large actual scores, current records and centered Live/playing count. Pregame projections are removed. A bottom line shows the latest observed point-change batch with separate observation/check times. It names a starter only when complete starter deltas reconcile to the official team-score change, and never invents a scoring play or an order within a multi-player poll. Logo thumbnails travel directly in the bounded ActivityKit payload; no new entitlement or shared cache is required. See the [implementation contract](league-extras-implementation.md) and [visual system](scoring-visual-system.md).

**42 model/artwork tests and two native layout tests passed** on iPhone 17 Pro Max / iOS 27 (`/tmp/mfl-activity45-final-tests.xcresult`). Tests cover single/multi-player and two-team changes, bench-only changes, missing totals, corrections, stale/recovered baselines, legacy decoding, 900-byte logo limits, payload budget and palette contrast. The final two layout tests passed again after screenshot review caught three-decimal score truncation; an OCR assertion now confirms the full rendered scores (`/tmp/mfl-activity45-layout-verified.xcresult`). Six native renders cover 343/408-point widths, light/dark environments, long names, large/negative scores and delayed content within the 160-point height. Screenshots were inspected. The earlier compact Scores/player-navigation UI journey also passed (`/tmp/mfl-activity45-tests.xcresult`); that bundle retains the initial height failure, subsequently corrected. These are focused checks, not a full app/UI, manual VoiceOver or physical Lock Screen/Dynamic Island audit. Real-logo presentation and live-game behavior on the phone await owner observation.

Both teams' current standings records also appear beneath owner names in the featured Scores card and matchup detail. Records include ties when present and omit unknown values. The earlier records-only change passed 18 MyTeamNavigationTests and one CompactScoresUITests journey (`/tmp/mfl-team-record-tests.xcresult`); it is included in this installation.

**Background scoring remains the next step.** Josh raised it September 13 after requesting the activity redesign. Build 45 still updates from the foreground app and becomes stale after 210 seconds. Reliable locked-phone updates need a server worker (potentially on the existing private Hephaestus host), MFL score access, ActivityKit push-token lifecycle handling, Apple push credentials/capability and real-device delivery validation. No APNs service or new remote credential storage was deployed. Apple's [ActivityKit push guidance](https://developer.apple.com/documentation/ActivityKit/starting-and-updating-live-activities-with-activitykit-push-notifications) describes that path; opportunistic iOS background refresh does not provide a game-day polling schedule.

## Previous installed private build — 0.6.3 (44), lineup Start/Bench recovery

Reproduced the reported full-lineup Start → extra starter → no eligible FLEX replacements dead end with a failing regression test. Start now opens a legal replacement picker when full; Replace includes Move to bench for existing overfilled drafts. Both actions remain draft edits followed by Review & submit. See [lineup behavior](lineup-starter-swaps.md).

All **272 app unit functions and eight native lineup journeys** passed on iPhone 16 Pro / iOS 18.4: 280 functions / 346 executions, no failures, skips or runtime warnings (`/tmp/mfl-lineup-bench-green.xcresult`). This includes full-lineup Start cancel/reopen/swap, direct Bench → fill, the exact overfilled FLEX dead end, prior starter/FLEX rotations and QB replacements. The failing pre-fix regression is retained at `/tmp/mfl-lineup-bench-red.xcresult`. Tests use Preview and in-memory repositories; no live lineup was submitted.

Screenshot review of the initial largest-text test found squeezed names despite passing tap assertions. Replacement rows now stack identity above projection/action at accessibility sizes. **All four new native journeys pass on the final app in dark appearance**, including the largest accessibility text size, with no failures, skips or runtime warnings (`/tmp/mfl-lineup-bench-verified-ui.xcresult`). The corrected assertion checks name-above-score placement because text reports its intrinsic bounds, not the available width. The existing FLEX replacement journey also passes with the final layout (`/tmp/mfl-lineup-bench-final-ui.xcresult`; that earlier bundle's large-text width assertion failed and was subsequently corrected). A single-method rerun executed zero tests and is excluded from passing evidence. Across the scoped runs, all 272 app unit functions and nine distinct native lineup journeys passed; this is not a rerun of every app UI journey or a manual VoiceOver/iPad audit. Final ordinary/largest-text dark screenshots were inspected, and simulator appearance was restored to light.

Signed Debug **0.6.3 (44)** built successfully and was installed over build 43 on Josh’s development iPhone September 10. `devicectl` confirms bundle version 44. App and Live Activity versions match, and the prior local NFL-test address and signing team were retained. Automatic launch was blocked by the phone lock; Josh can open the installed app after unlocking. This confirms installation, not a live Week 1 owner check. The update preserves the existing app container and private drafts; no live lineup was submitted. No GitHub push/merge or TestFlight distribution is claimed.

## Previous installed private build — 0.6.3 (43), reviewed NFL player IDs

September 9, 2026: the historical NFL test now supports **Review MFL player match** from a player’s stats. It loads a selected provider profile and the existing cached MFL catalog, suggests position-compatible candidates without preselecting anyone, and requires an explicit identity check/note before saving. Saved pairs are accessible through **NFL stats test → Reviewed player matches**, with removal available there and in the player review. Records include source IDs, identity evidence, seasons, review/source timestamps and a service namespace. One-to-one collision checks prevent silent replacement; changed identities require review again. Preview stays in memory and cannot create real matches.

This is a human-reviewed test mapping, **not an authoritative automatic crosswalk**. No real ID pair was auto-created, no current-season stats were attached to MFL cards, and MFL scores/actions/Live Activity remain unchanged. The provider’s ID-only bio lookup works for a known participant in the verified 2024 game; it is not labeled as a historical roster snapshot. The initial combined ID/season query returned a generic provider error; its precise cause was not retained. All profile calls use the existing private cache/quota system (8 of 20 attempts observed for September 10 UTC). No paid plan, polling, public endpoint or MFL data transfer to Hephaestus was introduced.

- **Tests:** 20 synthetic service tests, 9 native client tests, 7 mapping tests and one expanded native UI journey pass. The UI checks duplicate suggestions, no automatic selection, disabled empty confirmation, note → save → remove → Back. Final evidence: `/private/tmp/mfl-nfl-mapping-final.log`, result `Test-MFLBlitz-2026.09.09_23-33-35--0500.xcresult`, on iOS 18.4. This is 37 focused tests, not a rerun of the entire application suite.
- **Visual:** final light-appearance synthetic match screenshot inspected after compacting profile/identity information; no clipped controls. Manual VoiceOver, largest text and iPad testing remain open.
- **Release isolation:** the Release simulator build succeeds (`mfl-nfl-mapping-release.log`); the test/mapping implementation remains wrapped in `#if DEBUG` and is not a release feature. This was a compile check, not a TestFlight upload.
- **Phone:** signed Debug **0.6.3 (43)** built, installed and launched normally on Josh’s development iPhone. The existing private address was supplied only through the local build setting. No test launch flags or real mappings were applied. Owner acceptance of this new review flow is still pending.
- **Documentation:** runbook, privacy, README, changelog and remaining plan updated locally. Existing unrelated changes preserved; no push, merge or TestFlight distribution is claimed.

Remaining: owner-reviewed real pairs, broader identity/alias/position-change coverage, accuracy/correction/overtime checks, an approved current-season feed and usage rights, then optional enrichment. See the [service contract](nfl-stats-service.md).

## Previous installed private build — 0.6.3 (42), free-plan NFL test

September 9, 2026: the approved historical test is wired to Hephaestus. The documented season-only query returns 335 games for 2024; game 13186 returns grouped player statistics. The private service successfully returned 61 players, and a cached reread left its provider count at 4. It has a persistent 20-attempt UTC-day cap, 24-hour historical cache, cooldowns, no polling and no public endpoint. Josh approved private key storage and Tailscale HTTPS; the remote key/cache permissions are 600, the user service is enabled, and Serve status confirms tailnet-only port 8443 with no Funnel configuration. No subscription or ACL change.

Build 42 adds DEBUG-only My Team → Settings → NFL stats test with separate historical game/player views, synthetic Preview, missing-versus-zero handling and no API key in the app. Real MFL player cards, points, ownership, writes and Live Activity remain unchanged. The free account still excludes 2026/live data. See [service contract, operation and remaining plan](nfl-stats-service.md).

Verification and delivery:

Phone follow-up (September 9): the owner reported a connection failure in the test screen despite VPN being active. Read-only checks confirmed a healthy server, HTTP 200 cached game list and phone Tailscale reachability. The owner confirmed Safari reaches the same endpoint. The catch-all app message hid whether the failure was HTTP, decoding or transport; the follow-up build distinguishes those safely, with eight passing client tests. Its explicit `--nfl-test-connection-check` mode requests only the cached NFL list, without MFL session restoration. After the owner unlocked the phone, this actual on-device Swift-client check succeeded: **Connected · 335 historical games**. The normal app was then relaunched without the diagnostic flag for the owner's Settings-path retry. The original failure was not reproduced and its cause remains unconfirmed; no VPN/ATS/trust setting was changed and no upstream retry loop was added.

- **Server:** 17 synthetic tests pass: persisted quota/cooldowns, coalescing, retry expiry, stale timestamps, null/overtime status, game/team/route validation, key permissions and HTTP access checks. The earlier standalone probe's 27 tests also pass.
- **App:** seven client safety tests and one native Settings → game → player → Back journey pass on iOS 18.4 (`mfl-nfl-test-final.log`). The client tests also passed on iOS 27. Early UI failures were test-selector/accessibility-query issues, corrected with exact tab selection and stat-row labels; the iOS 27 result collector subsequently stalled, so final native evidence is from iOS 18.4. No final iOS 27 UI pass is claimed.
- **Real service:** the app's actual Swift client validated 335 games and 61 players, with an unchanged original cache timestamp on reread. After a deliberate cache-version refresh for overtime handling, service usage is 5 requests for the UTC day. No 2026/live upstream was queried by the app/service.
- **Visual:** the passing light-appearance synthetic player-stat screenshot was inspected. Values, zeroes, dashes and historical/source labels are readable. Full VoiceOver and large-text/iPad checks remain open.
- **Phone:** signed Debug **0.6.3 (42)** built, installed and launched on Josh's development iPhone. The private server URL is prefilled through a local build setting and verified in the built Info.plist; no key or private hostname was committed. Josh confirmed both the normal Settings game list and the game → player-stat breakdown load successfully, then confirmed his sample accuracy check. This is owner-reported phone/Tailscale and sample acceptance, separate from automated tests. Specific players/fields and the comparison source were not supplied; broader accuracy, corrections and live-feed qualification remain open.
- **Delivery:** docs, privacy policy, service runbook and remaining plan updated locally; `git diff --check` passes. No GitHub push/merge or TestFlight distribution is claimed for this increment. The pre-existing dirty worktree is preserved.

## Previous installed private build — 0.6.3 (41), scoring symmetry and bench preview

Installed and launched September 9, 2026. Owner screenshots from build 40 showed two left-aligned hero halves and league score blocks sitting above their team identities. Build 41 mirrors the hero's equal-width halves, aligns their metric baselines independently of wrapped names, and centers league actual/projection blocks beside identities. Temporary badges use the existing row padding without moving content; accessibility retains an in-flow line. Player numeric columns have a common minimum width, and NFL context now reads `ATL @ DAL`, with no separator dot between teams. Day/time remains together below.

The owner also requested a collapsed bench summary. Bench now shows a separate subtotal for each team below its disclosure instead of the non-counting explanation. Only reported finite bench points contribute, and incomplete/ambiguous data stays a dash. Official matchup totals and API requests are unchanged.

- **Final verification:** all 249 app unit functions plus three focused native journeys pass on iOS 18.4 (`/tmp/mfl-scoring-build41-final18.xcresult`): 252 functions, zero failures/skips. The same three final journeys pass on iOS 27 (`/tmp/mfl-scoring-build41-final27.xcresult`): compact density/44-point targets and mirrored metrics, bench total/expand/collapse, and stable recent-change feedback. The new unit function covers real zero, negative, missing, duplicate and unclassified bench scoring without changing official totals.
- **Additional checks:** the wider eight-journey runs verified the five other live/final/pregame player and large-text/delayed-game-info paths on both versions (`/tmp/mfl-scoring-symmetry-bench18.xcresult`, `/tmp/mfl-scoring-symmetry-bench27.xcresult`). Those runs each failed the new bench accessibility-group lookup; corrected grouping and the final three-journey reruns above resolve it. An earlier narrow-screen density failure was fixed by reducing the reserved numeric-column width without shrinking text. These earlier runs are not counted as full passes. Core code is unchanged since build 40's 108 passing tests.
- **Visual review:** inspected final dark iOS 18 and light iOS 27 screenshots for mirrored hero/league alignment, player rails, change badges and collapsed bench totals. Large-text phone layouts were checked in the broader run. Synthetic preview data is not real game-week acceptance; manual VoiceOver/Reduce Motion, iPad and owner live checks remain open.
- **Phone:** signed **0.6.3 (41)** installed and launched on Josh's iPhone 17 Pro Max, verified through CoreDevice (`/tmp/mfl-scoring-build41-install.json`, `/tmp/mfl-scoring-build41-installed.json`, `/tmp/mfl-scoring-build41-launch.json`). Deep/strict signature verification passes; signed build log is `/tmp/mfl-scoring-build41-device-final.log`. No Preview arguments, live league writes or TestFlight distribution were used.
- **Source/release:** preserved the existing dirty checkout and excluded duplicate files. No commit, push or merge is claimed. The roadmap retains owner Week 1 validation, accessibility/device review and existing release gates; TestFlight remains on hold.

## Previous installed private build — 0.6.3 (40), scoring visual system and contextual player view

**Installed and launched September 9, 2026; not pushed or merged.** The Patriots search correction is implemented: NFL names/nicknames/common codes match individuals; explicit league position rules exclude unused MFL team-unit records. Champion Hall uses QB/RB/WR/TE; uncertain rules retain a broader catalog. No query-time requests or ownership/action changes.

The owner approved the [scoring visual system](scoring-visual-system.md), including the native San Francisco font. It is implemented across Scores, the paired matchup table and Live Activity, replacing the density-only prototype. Active/finished players open a Week N / Player segmented view; pregame players retain the card. Matchup captions keep the day/time together below team/opponent, with a stacked fallback at narrow widths. Change badges reserve their height, and recent changes appear below scoring lists without moving visible rows.

- **Core:** all 108 tests / 13 suites pass (`/tmp/mfl-core-scoring-system-second.log`). Scoring receipts preserve actual response timestamps through cached/coalesced reads; typed offline errors remain concise. The new display model tests cover actual zero/missing values, stale/offline/recovery, same-check suppression, negative/final changes, scopes and bounded history. No new polling cadence or API provider was introduced.
- **Final-source verification:** all 248 app unit functions and nine focused native journeys pass on both iOS 18.4 and iOS 27 (`/tmp/mfl-scoring-system-refined18.xcresult`, `/tmp/mfl-scoring-system-refined27.xcresult`): 257 functions per run, zero failures/skips. These checks include live/final Week / Player entry, pregame card entry, missing stats, recent changes, compact targets, largest text, day/time grouping, optional delayed game info and one-Back player/team/FLEX navigation.
- **Broader earlier run:** all 248 app unit functions and 15 focused native journeys, including all six search journeys, pass on both versions (`/tmp/mfl-scoring-system-final18.xcresult`, `/tmp/mfl-scoring-system-final27.xcresult`). These predate the final grouped-time/stable-badge refinements; final-source scoring/layout checks are recorded separately above. Earlier prototype and failed-iteration runs are not counted as final passes.
- **Visual review:** final iOS 18 dark and iOS 27 light screenshots inspected at normal/largest text, including pregame kickoff and the contextual week summary. Day/time stays together while accessibility layouts stack the comparison. Preview players, scores and stat lines are synthetic, not real league activity. Manual VoiceOver, Reduce Motion and iPad qualification remain open. The existing nonfatal AppIntents metadata-extraction warning remains; no AppIntents integration was added.
- **Phone:** signed **0.6.3 (40)** installed and launched on Josh’s iPhone 17 Pro Max. Deep/strict signature verification passes. CoreDevice confirms the exact version/build and normal launch without Preview arguments (`/tmp/mfl-scoring-system-install.json`, `/tmp/mfl-scoring-system-installed.json`, `/tmp/mfl-scoring-system-launch.json`). Signed build log: `/tmp/mfl-scoring-system-device-refined.log`. No live league writes or TestFlight upload were performed.
- **Source/release:** changes remain in the existing dirty `codex/hobday-craft-fixes` checkout, preserving adjacent work and excluded duplicate files. No commit/push/merge is claimed. Owner Week 1 acceptance, real game-state/correction/Live Activity checks and existing release gates remain; TestFlight stays on hold.

## Previous installed private build — 0.6.2 (39), global player search

Installed and launched September 9, 2026. A far-right magnifying glass on all five primary tabs and matchup detail opens a shared **Search players** sheet. Results include current fantasy team/owner/status, and the existing player card returns to the same query. Close preserves the originating tab/matchup. See the [search contract](player-search.md).

- **Performance/safety:** reuse the daily decoded/disk-backed player catalog, build a local index, filter off the main actor, and load complete current ownership separately. No per-keystroke/per-result network calls or new startup requests. Missing ownership stays unknown; free agency requires an explicit supported pool entry. Existing add/drop/trade review and fresh-write gates are unchanged. Recents stay in session memory and clear on scope/sign-out.
- **App verification:** all **235 app unit tests / 23 suites** pass on iOS 18.4 (`/tmp/mfl-player-search-final18.xcresult`). This includes 11 new search test functions covering a 20,000-player catalog, cache/request counts, incomplete and duplicate roster responses, multi-owner status, freshness, session replacement and failure states. Core sources are unchanged; the earlier 107-core-test result is historical, not a new run.
- **Native verification:** all five search journeys pass on both iOS 18.4 and iOS 27, zero test failures (`/tmp/mfl-player-search-final18.xcresult`, `/tmp/mfl-player-search-final27.xcresult`). They exercise every primary-tab entry point, matchup return, player Back/query restoration, delayed ownership, failed ownership, and largest accessibility text. Light iOS 27 and Dark iOS 18/27 screenshots were inspected; largest text wraps and needs scrolling while the keyboard is open. These checks do not replace manual VoiceOver or iPad review.
- **Corrections during verification:** native search originally hid Close while typing and cleared the query on player navigation. Both are corrected and covered by passing regressions. Earlier failed runs are not counted as passes. Xcode reports the existing nonfatal AppIntents metadata-extraction warning; no AppIntents integration was added.
- **Phone:** signed **0.6.2 (39)** installed and launched on Josh’s iPhone 17 Pro Max. CoreDevice verifies the exact version/build (`/tmp/mfl-player-search-install.json`, `/tmp/mfl-player-search-installed.json`, `/tmp/mfl-player-search-launch.json`). Deep/strict signature verification passes with access to certificate trust services. No Preview arguments or live league writes were used on the phone.
- **Source:** implemented in the existing dirty `codex/hobday-craft-fixes` checkout, preserving build-37/38 work and excluded duplicate source files. No commit, push or merge is claimed for this increment.
- **Remaining:** owner Week 1 search/ownership acceptance, manual VoiceOver and iPad-specific search layouts, plus the existing game-week and release gates. TestFlight remains on hold.

## Previous installed private build — 0.6.1 (38), adaptive continuity verification

Preserves all prior craft behavior. Xcode target membership now excludes three older duplicate Swift files found in synchronized source folders; the files and their bytes remain intact. A new synthetic lineup journey checks draft and review continuity across short landscape and portrait layouts. See [adaptive layout validation](adaptive-layout-validation.md).

- **Verification:** final generic app/test build passes (`mfl-duo-final-build2.log`). The final rotation journey passes with zero failures in 36.093 seconds (`mfl-duo-resize-final.xcresult`); inspected full-screen landscape and portrait review screenshots show reachable actions and the same nine-starter draft. Earlier 107 core / 224 app counts below are prior-pass evidence; this increment changes project membership, version, tests and documentation.
- **Phone:** signed **0.6.1 (38)** installed September 9; CoreDevice confirms version/build (`mfl-duo-install.json`, `mfl-duo-installed.json`). Deep/strict signature verification passes. iOS denied automatic launch because the phone is locked (`mfl-duo-launch.json`); open Blitz after unlocking. No Preview arguments or live league writes were used on the phone.
- **Source:** uncommitted delta over the existing `codex/hobday-craft-fixes` checkout at `bcf86cbd448e90eaaa0e9bb709d2aa79a499e00a`. Only reviewed paths are integrated after matching their original baseline bytes. No commit, push or merge.
- **Remaining:** actual iOS 27.1 Duo folding, vertical bars, reserved regions and system presentations; the existing hardware/accessibility, real game-week and release gates still apply. Current-SDK rotation verification does not establish Duo hardware qualification.

## Previous installed private build — 0.6.1 (37), interface craft

All six [craft refinements](craft-refinements.md) are implemented and installed on Josh’s iPhone September 9: open Board detail survives summary refreshes with inline loading/Retry; an owner-specific projected points margin replaces Win outlook; missing owner games are explicit; week loads publish independently; scores and headers adapt for accessibility text; both week controls share league settings and known-week fallback behavior.

- **Source:** uncommitted changes on `codex/hobday-craft-fixes`, based on `bcf86cbd448e90eaaa0e9bb709d2aa79a499e00a`. Tested app sources and the integrated checkout were compared byte-for-byte before the signed build. No commit, push or merge is claimed for this increment.
- **Core:** all **107 tests / 13 suites** pass (`mfl-hobday-implementation-core.log`).
- **App:** all **224 tests / 22 suites** pass on iOS 18.4 (`mfl-hobday-final-units2.xcresult`). Coverage includes independent suspended week reads, session/week guards, empty and partial week settings, missing owner matchups, exact margin orientation, Board continuity and older reads arriving after reply readback.
- **Native UI:** five relevant journeys pass across focused runs: Scores week control, Lineup week control, matchup navigation, Board saved-reply recovery and largest-text Scores. The final Board and largest-text journeys both pass on the final build in Dark Mode (`mfl-hobday-final-ui.xcresult`, 2 tests, zero failures). Three final screenshots were inspected: readable status/header, stacked teams and scores, and the signed projected margin. The other three journeys passed in the earlier compatibility run; this does not claim the full native suite was rerun.
- **Development corrections:** model tests exposed empty schedule fallback and reopened start-only week settings; both were corrected with regressions. Review caught an older Board read replacing fresh reply readback; request generations now reject it. An earlier UI run lost characters during synthetic typing; the final serial run preserved the exact input before save and after resume without changing the composer. Screenshot review led to stacked status/header layouts at accessibility sizes. Interrupted simulator/diagnostic attempts are not counted as passes.
- **Phone:** signed **0.6.1 (37)** installed successfully, and CoreDevice confirms version/build (`mfl-hobday-install.json`, `mfl-hobday-installed.json`). Deep/strict signature verification passed. Automatic launch was denied because the phone was locked (`mfl-hobday-launch.json`); open Blitz after unlocking. No Preview launch arguments or live league mutations were used on the phone.
- **Remaining:** manual VoiceOver, live league week rollover and the existing hardware/release checks. TestFlight remains on hold. Synthetic Preview verification and successful installation do not establish owner acceptance.

## Previous installed private build — 0.6.0 (36), matchup opponent and kickoff

Adds NFL opponent and device-local kickoff to matchup player rows, replacing “Yet to play” when the shared schedule is available. Live/Final uses the MFL player clock; only an explicit matching-week bye is called a bye. Starting lineups explains the time zone once. Scores and navigation remain visible while optional availability loads, without a per-player spinner or request. Scope/week checks, existing API caching and single-stack navigation are preserved.

- **Source:** final app/test source `1e423ae939bbb0608380a10e3ee4174813ee5ca9`; the change after `74d30f3` only adjusts screenshot/navigation order in a native test, not installed app code.
- **Core:** all 107 tests / 13 suites pass (`mfl-build36-core-clean.log`). The first local attempt hit signing metadata in the existing build directory; a clean temporary scratch build passed without altering the package.
- **Focused verification:** all 215 app unit functions plus three native journeys pass on iOS 27 and iOS 18.4: 218 functions / 272 executions per run, zero failures/skips/runtime warnings (`mfl-build36-verified27.xcresult`, `mfl-build36-final18.xcresult`). The final iOS 18 run includes the test-only screenshot adjustment; the full iOS 27 run is recorded separately below. Coverage includes local/DST time formatting, missing/wrong-week cache data, explicit byes, Live/Final boundaries, a held 20-second optional schedule, largest text and one-tap player/Back routing. Light/Dark screenshots and wrapped largest-text game captions were inspected.
- **Development corrections:** early unit checks exposed nil-week being mistaken for a bye and an overly strict leading-zero expectation for localized time. Both were fixed. The first expanded iOS 18 screenshot test scrolled the player tap under the navigation bar; moving its navigation check before screenshot scrolling passed without changing app code.
- **Phone:** signed **0.6.0 (36)** installed and successfully launched September 7 (`mfl-build36-installed.json`, `mfl-build36-launch.json`). Signature verification passed. No Preview launch arguments or live league mutations were used on the phone.
- **Full local regression:** all 215 app unit functions and all 47 native journeys pass on exact source with iOS 27: 262 functions / 316 executions, zero failures/skips/runtime warnings (`mfl-build36-full27.xcresult`). Native journeys took 1,379 seconds and include the two-week offline simulation, Lineup/FLEX, draft recovery, player/Back, Watchlist, roster action safety, Trading Block and Calendar.
- **GitHub:** exact-head [CI run 34165993833](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34165993833) passed all 107 core tests, 215 app unit functions and all 47 native journeys. The iOS job completed in 39 minutes 35 seconds; native tests took 1,928 seconds with zero failures. Its proximity to the 40-minute job cap is recorded as a CI runtime/headroom follow-up in the roadmap, not an app-performance measurement.
- **Merge:** [PR #11](https://github.com/biggsjm/mfl-blitz-ios/pull/11) squash-merged as [1a7983a](https://github.com/biggsjm/mfl-blitz-ios/commit/1a7983a61914c2a6510ba12976c4f43ef86dd837) September 7 at 5:55 PM Central. The final completion record changes documentation only; installed app source and the exact tested app/test source above remain unchanged.

Week 1 owner opponent/kickoff and Live → Final checks remain open. Synthetic tests and successful launch are not live-week certification. TestFlight stays on hold until iOS 27 and macOS 27 leave beta and Josh approves distribution. No live league writes are part of this display-only change.

## Previous installed private build — 0.6.0 (35), remove the last trading-block player

Josh found that build 34 could not save an empty existing block. Build 35 separates that intentional removal from a blank new draft: demote the last player, **Review removal**, then **Remove listing**. The review states that the listing and Looking for note will clear while every player stays on the roster. Empty removal drafts survive save/resume, and Cancel preserves the staged edit. Settings remains at the upper-left of My Team.

The explicit removal entry point sends one trade-bait POST with both fields present and empty. Fresh owner membership/TRADES permission and unchanged-baseline checks remain mandatory. A durable empty intended-list marker precedes the request; only a fully parsed fresh export with no owner listing or empty assets **and** needs confirms removal. Failed reads, unchanged data and timeouts never falsely report success or replay the write. MFL's official form documents full replacement but not empty-list semantics; Josh confirmed September 7 that last-player removal works on MFL. This is owner-reported acceptance, not a live mutation performed by automated QA.

- All **107 core tests / 13 suites** pass (`mfl-build35-core.log`).
- **Exact app/test source:** `3663a37789816fb7cca28b475cb6d28f8c6d680e`.
- All **209 app unit functions and five native journeys** pass on both iOS 27 and iOS 18.4: 214 functions / 268 executions each, zero failures/skips/runtime warnings (`mfl-build35-removal27.xcresult`, `mfl-build35-removal18.xcresult`). Coverage includes absent/empty removal readback, retained ownership, permission/conflict rejection, saved empty drafts and timeout/relaunch no-replay recovery. The extended native editor journey verifies last-player demotion, saved removal draft, Cancel, explicit confirmation and return to the empty state. Actual Light/Dark removal review screenshots were inspected.
- **Phone:** signed **0.6.0 (35)** installed September 7; CoreDevice confirms the build (`mfl-build35-installed.json`). Signature verification passed. Automatic launch was denied by the device lock (`mfl-build35-launch.json`); open Blitz after unlocking. No live listing was removed for QA. The initial signed-build invocation omitted the development-team override and failed before installation; the corrected invocation with Josh's configured team succeeded.
- **Full local regression:** all **209 app unit functions and all 45 native journeys** pass on exact source with iOS 27: 254 functions / 308 executions, zero failures/skips/runtime warnings (`mfl-build35-full27.xcresult`). Native journeys took 1,309 seconds, including the complete two-week offline Preview flow, Lineup/FLEX, Scores/player Back, drafts, standings, IR/add/drop, Calendar and last-player block removal.
- **GitHub:** exact-head [CI run 34160057274](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34160057274) passed all 107 core tests, 209 app unit functions and all 45 native journeys using Xcode 16.4 / iOS 18.5. The iOS job completed in 31 minutes 6 seconds; native tests took 1,464 seconds with zero failures.
- **Merge:** [PR #10](https://github.com/biggsjm/mfl-blitz-ios/pull/10) squash-merged as [e946b1c](https://github.com/biggsjm/mfl-blitz-ios/commit/e946b1cb571119ec7679e8651b5cb13a2cf47522) September 7 at 4:09 PM Central after both exact-head checks passed. The completion record changes documentation only; app/test source remains the tested and installed `3663a37`.
- **Owner confirmation:** Josh confirmed the last-player removal fix works on MFL September 7. That closes this specific build-35 validation item, not every publication, reminder, Live Activity or game-week scenario. The earlier automatic-launch denial remains the historical tool result; subsequent use is owner-reported.
- **Remaining:** broader intended block publication/update and exact MFL comparison, notification permissions/delivery, changed commissioner deadlines, Apple Calendar handoff, active-game ActivityKit lifecycle and broader manual accessibility/device checks. TestFlight remains on hold until both OS betas end, the existing release gates pass and Josh approves. Continuous closed-app Live Activity scoring still needs a separately approved APNs service.

## Previous installed private build — 0.6.0 (34), lineup-style Trading Block and My Team Settings

Josh tested build 33 and requested roster-to-block promotion/demotion like Lineup, plus moving Settings from Scores to My Team. Build 34 replaces the separate asset picker with **On the block / Your roster**, green up/orange down controls, optional needs text and a pinned **Review & submit trading block** action. Review is a separate, cancelable sheet with an explicit final submit; moving a row never changes MFL roster membership or lineup starters. Owned draft picks remain in a secondary disclosure. The Settings gear is now top-leading on My Team, absent from Scores.

The initial older-Xcode CI run for build 33 stopped at notification SDK Sendable annotations before running iOS tests. The notification import now bridges those legacy annotations while retaining actor-owned scheduling; build-34 CI compilation passes. A local older-iOS run also identified an obsolete inbox geometry assertion after adding the Offers / Trading Block selector; the test now requires Create trade directly below the selector, not immediately below navigation. No workflow assertion or safety check was removed. A first native submission test caught a missing receipt during nested-sheet dismissal; the sheets now close in order and the parent presents its queued notice only after dismissal completes.

- **Exact app/test source:** `94628af49abd21a74203c303fb9300f742acbe42`.
- **iOS 27:** all 204 app unit functions plus seven focused native journeys pass: 211 functions / 263 executions, zero failures/skips/runtime warnings (`mfl-build34-verified27.xcresult`). This covers block promotion/demotion, staged review/cancel/submit/readback, draft recovery/protection, Calendar/reminder navigation, largest-text editor access, My Team Settings and the Scores/empty-inbox toolbar regressions. Actual Dark Mode editor, review and reminder screenshots were inspected. Native runtime was 202 seconds.
- **Core:** unchanged from build 33; all 106 tests / 13 suites pass locally and in exact-head CI.
- **Phone:** the app and embedded Live Activity extension both pass signature verification and report build 34. Signed **0.6.0 (34)** installed on Josh's iPhone September 7 (`mfl-build34-installed.json`). Automatic launch was denied by the device lock (`mfl-build34-launch.json`); launch/owner acceptance is not yet claimed.
- **Older-iOS:** the final full iOS 18.4 suite passes all 204 app unit functions and all 45 native journeys: 249 functions / 301 executions, zero failures/skips/runtime warnings (`mfl-build34-verified18.xcresult`), 1,075 seconds overall. Exact-head [CI run 34158249517](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34158249517) was superseded/cancelled by the requested build-35 fix; it is not passing merge evidence. Interrupted or superseded local runs are not final passing evidence.

## Previous installed private build — 0.6.0 (33), Trading Block, Calendar and Live Activity

Josh approved plan items 1–3 and added the current-week matchup Live Activity. The [implementation contract](league-extras-implementation.md) records native navigation, fresh publication/readback, durable draft/uncertain-write recovery, protected optional-feed caches, exact calendar occurrences and notification lifecycle behavior. The [approved plan](trading-block-calendar-plan.md) records remaining scope. No TestFlight upload is authorized before the OS-beta hold and existing release gates are lifted.

- Owner-provided empty/singleton trading-block JSON verified `willGiveUp` / `inExchangeFor`; the attached ICS verified explicit repeat dates and the November DST shift against JSON anchors. Private exports are not committed; fixtures are synthetic.
- **Local verification:** all 106 core tests / 13 suites pass (`mfl-build33-core-final.log`). Final source `24d64fc` passes all 204 app unit functions and four new native Preview journeys on iOS 27 in Dark Mode: 208 functions / 260 executions, zero failures/skips/runtime warnings (`mfl-build33-confirmed27.xcresult`). Actual screenshots were inspected; reminder confirmation stays visible in the toolbar. Read-only execution of the compiled parser against Josh's attachment verified all nine JSON anchors, 57 occurrences, and noon Central deadlines across CDT/CST (`mfl-verify-calendar`).
- **Phone:** signed **0.6.0 (33)** installed and launched September 7; CoreDevice confirms version/build (`mfl-build33-installed.json`, `mfl-build33-launch.json`). No preview launch arguments or live mutation were used. Owner feedback on the Trading Block editor prompted build 34.
- **GitHub:** [PR #10](https://github.com/biggsjm/mfl-blitz-ios/pull/10) contains the implementation, plan and documentation. Initial [run 34157196227](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34157196227) passes core but fails older-SDK iOS compilation; it is not a passing release run. Merge awaits build-34 exact-head checks.
- Entire-block removal and new cash listings remain on MFL. No commissioner calendar editing, inferred lineup-review deadline, calendar subscription or raw NFL statistics is added.
- Live Activity updates are on-device/foreground only, stale after two minutes without an update. There is no APNs backend or promise of continued scores while Blitz is closed. Real active-game appearance, permission denial/delivery and Apple Calendar insertion remain owner/device checks; automated QA makes no real league, calendar or notification changes.

## Installed baseline — 0.5.4 (32), cancelled Scores requests

Josh's September 7 screenshot shows URLSession `NSURLErrorCancelled` (`-999`) from live scoring displayed as a raw error dump. The transport preserved Swift `CancellationError` but wrapped URLSession cancellation as `MFLCoreError.transport`; Scores then presented it as a failed refresh. Build 32 normalizes both typed forms, quietly retains existing Scores data/warnings on cancelled manual/polling/full reads, releases loading gates, and does not delay foreground recovery after an interrupted full refresh. Real transport failures use concise copy without raw URL/UserInfo; internal wrapping keeps only a numeric code. There is no blanket suppression of mutation errors or replay of cancelled writes.

The new core tests reproduced the wrapping/diagnostic leak; the new app tests reproduced false alerts/stale warnings. One initial function-name-filtered app invocation selected zero tests and is not verification; the full Reliability suite then reproduced all four regression functions before the fix.

- **Exact source:** `14cc99b148af24b7508bb2a2ce2b42189054f3ae`.
- **Local checks:** all 97 core tests / 12 suites pass (`mfl-build32-core-final.log`); all 193 app unit functions plus four native Scores/player/cached-startup journeys pass on iOS 27: 197 functions / 249 executions, zero failures/skips/runtime warnings (`mfl-build32-verified.xcresult`). Safety coverage includes uncertain-write/readback behavior and the offline two-week model simulation.
- **Phone:** signed 0.5.4 (32) installed September 7. CoreDevice confirms the version/build (`mfl-build32-installed.json`). Automatic launch was denied by the phone lock (`mfl-build32-launch.json`); open the installed app after unlocking. Installation is not owner acceptance or a real-network performance measurement.
- **Full older-iOS regression:** all 193 app unit functions and all 40 native journeys pass on iOS 18.4: 233 functions / 285 executions, zero failures/skips/runtime warnings (`mfl-build32-ios18.xcresult`). Native execution took 946 seconds, including the complete offline two-week manager journey.
- **GitHub:** exact-head [run 34145452711](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34145452711) passed the full core and iOS jobs: 97 core tests, 193 app unit functions and 40 native journeys. The Xcode 16.4 iOS job completed in 28 minutes 10 seconds; its native tests took 1,371 seconds, zero failures.
- **Merge:** [PR #9](https://github.com/biggsjm/mfl-blitz-ios/pull/9) squash-merged as [630b6ba](https://github.com/biggsjm/mfl-blitz-ios/commit/630b6baf82907030b42c49f3807eda426a5155cb) September 7 at 12:25 PM Central. The final completion record changes documentation only; app/test source remains the exact tested and installed `14cc99b`. No live league writes were used for QA.
- **Owner/release checks:** Josh confirmed the Scores cancellation fix works September 7. Player opening speed, physical profiling, real Week 1 scoring/writes and broader manual accessibility/device checks remain open. Josh will validate Week 1 on his dev device; TestFlight is intentionally on hold until both iOS 27 and macOS 27 leave beta, followed by the remaining release gates and his go-ahead. No release date is assumed.
- **Subsequent work:** Josh approved Trading Block, League Calendar and reminders, plus a Live Activity; see the build-33 candidate above. Polls and playoff brackets remain queued.

## Previous installed private build — 0.5.4 (31), immediate player identity

Josh confirmed build 30 removes the lingering game-log/game-info spinner but reported remaining card latency. The primary view still waited for ownership before rendering its known identity. Build 31 carries display-only identity from each canonical player route, rejects player/scope mismatches, and renders it before ownership completes. Week metrics and research no longer depend on that response. No ownership claim or roster action is authorized by the preview; full-detail errors retain readable identity. No new API request, persistence, permission or provider is added.

A deterministic offline Preview journey holds ownership for 20 seconds and verifies identity/Week content before the response, with actions unavailable, then checks that status fills in. Final app/test source is `dec8795a85fa202f90254ee23c973fc06c0594c1`.

- **Core:** all 93 tests pass on the unchanged final core source and in exact-head GitHub CI.
- **Full local regression:** all 189 app unit functions and all 40 native journeys pass on iOS 27: 229 functions / 274 executions, zero failures, skips or reported runtime warnings (`mfl-build31-full.xcresult`). UI execution took 1,145 seconds, including the two-week offline Preview journey.
- **Older-iOS checks:** all 189 app unit functions and the held-ownership, cached-startup and matchup/Back journeys pass on iOS 18.4 (`mfl-build31-primary18.xcresult`). All ten focused player/navigation journeys also pass in Dark Mode, including largest-text action safety (`mfl-build31-dark18.xcresult`), with zero failures/skips/runtime warnings. Actual Light/Dark and iOS 27 before/after screenshots were inspected.
- **Phone:** signed **0.5.4 (31)** installed on Josh's iPhone September 7; CoreDevice confirms its version/build (`mfl-build31-installed.json`, `mfl-build31-install.json`). Initial and final automatic launch attempts were denied because the phone was locked (`mfl-build31-launch.json`, `mfl-build31-final-launch.json`). Open the installed app after unlocking; launch and live card-latency acceptance are not claimed.
- **GitHub:** [run 34141337310](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34141337310) passed on exact `dec8795`: 93 core tests, all 189 app unit functions and all 40 native journeys, zero failures, using Xcode 16.4. The iOS job completed in 30 minutes 25 seconds, within the new 40-minute limit.
- **Merge:** [PR #8](https://github.com/biggsjm/mfl-blitz-ios/pull/8) squash-merged as [79ec9fd](https://github.com/biggsjm/mfl-blitz-ios/commit/79ec9fdf86b7468fc64f5fb7fb9c047e44ad57a5) September 7 at 11:34 AM Central. The merged tree exactly matches the tested/installed source. This completion record changes documentation only.
- **Documentation and remaining work:** README, API/performance/design notes, contributing guidance, changelog, execution plan and owner checklist are reconciled. Build-29 Back navigation and the build-30 spinner fix are owner-confirmed; build-31 opening speed, real Week 1 scoring/writes, physical profiling, broader manual accessibility/device checks and Week 2 distribution gates remain open. Queued features 6–9 are not started. Automated QA made no live league changes.

## Previous installed private build — 0.5.4 (30), player cards and loading

The [player-card design](player-detail-ux.md) consolidates identity, season points/average and roster/health status; current-week information shares one card; a visible four-week-at-a-time log shows Week / Points / NFL opponent, and biography is collapsed. Josh explicitly approved current-NFL-team schedule context for historical opponents with the caveat behind a small information button. MFL's API does not provide raw NFL statistics. Drop is now a labeled secondary action with explicit removal-not-bench confirmation; own-team status is not another team link.

Matchup detail adds secondary owner names and removes repetitive positional subtotals/acronyms. Baseline native tests reproduced the duplicate-matchup/Back-to-player bug on iOS 27 and 18.4. Typed live-matchup routes fix it, and a stack-scoped router keeps pushed team shortcuts functional. Two independent AI review perspectives inspected code and actual native screenshots; this is not a human usability study.

Build 30 additionally removes optional biography from the normal primary read, avoids forced ownership refresh on ordinary reappearance, and shares game-info reads independently of the first screen's cancellation. Interrupted reads leave an actionable retry state rather than an idle spinner. Signed app source `e1905397ea0777e5022b807c70d18e48553a44de` installed and launched on Josh's iPhone September 7 (`mfl-build30-installed.json`, `mfl-build30-launch.json`); CoreDevice confirms 0.5.4 / 30. Josh confirmed the spinner is fixed; the remaining primary-card latency is tracked in build 31 above. All 93 core tests pass on the unchanged final core source. Exact `e190539` passes all 188 app unit functions and all 39 native journeys on iOS 27 (`mfl-build30-full.xcresult`), plus nine focused iOS 18.4 journeys and three Dark Mode/large-text journeys (`mfl-build30-player-ui18.xcresult`, `mfl-build30-dark18.xcresult`). Actual screenshots were inspected. The seven new unit regressions cover shared/cancelled reads and lazy biography. This work is included in the build-31 merge recorded above. Earlier development runs caught a shortcut-grid navigation regression and an assertion treating native menu row sizing as a custom button size; the grid retains explicit typed-path buttons and menu tests exercise actual native selections. Failed/superseded runs are not final evidence. No live league writes were used for QA.

GitHub run [34135836510](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34135836510) passed 93 core tests and all 188 app unit functions but failed four of 39 native journeys: Xcode 16.4 / iOS 18.5 could not find accessibility hit points for visible toolbar menus and a dimmed non-actionable navigation title. The test-only correction uses measured native touches after asserting visible navigation-bar bounds, retaining all real menu/action and cancellation checks. The 28-minute job was not timed out; its limit is increased to 40 minutes to leave room for the expanded full suite. This failed run is not final compatibility evidence. No app code changes after the installed `e190539` are part of this correction. All four corrected journeys pass twice on iOS 18.4 (`mfl-build30-ci-touch18.xcresult`); all eight player journeys pass on iOS 27 (`mfl-build30-ci-touch27.xcresult`). The complete 39-journey iOS 18.4 run also passes on test head `d99dff2`, zero failures in 917 seconds (`mfl-build30-full-ui18.xcresult`).

Build-30 GitHub run [34138964190](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34138964190) was superseded/cancelled when build 31 was pushed. Its log reports all 188 app unit functions and all 39 native journeys passing, but the overall run was cancelled and is not counted as the final GitHub pass. Final compatibility and merge evidence are the build-31 receipt above.

## Previous installed private build — 0.5.4 (29), owner feedback

Signed source `afe29ff97942d4a2257d9fa5708b68424cfe2953` installed and launched on Josh's iPhone September 7; CoreDevice confirms 0.5.4 / 29 (`mfl-build29-final-installed.json`, `mfl-build29-unlocked-launch.json`). Josh confirmed Scores → matchup → player → Back works. He then reported slow player loading and a lingering game-info spinner; build 30 subsequently fixed the spinner, while build 31 addresses the remaining card latency.

- Final build-29 core source passes 93 tests / 12 suites (`mfl-build29-core-fresh.log`).
- All 181 app unit functions and three focused native journeys pass on exact `afe29ff` in Dark Mode / iOS 18.4 (`mfl-build29-readable-dark18.xcresult`); the same three native journeys pass in Light Mode (`mfl-build29-readable-light18.xcresult`). Actual screenshots were inspected, including the improved popover contrast and large-text log headers.
- Earlier integrated runs pass all 181 app unit functions plus 11 focused journeys on both iOS 27 and 18.4 (`mfl-build29-feature.xcresult`, `mfl-build29-feature18.xcresult`). The full iOS 27 regression passes all 181 app unit functions and 39 native journeys (`mfl-build29-full.xcresult`), before the final owner-spacing/accessibility-only adjustments. These are not build-30 verification.
- GitHub runs `34133876643` and `34134511994` were superseded/cancelled; neither is counted as final merge evidence. No automated QA submits live league changes.

## Previous installed private build — 0.5.3 (28), cached startup

September 7, 2026. [Performance review](performance-startup.md) documents the implemented cached-launch path, scores/lineup-first request ordering, protected daily league metadata, shared player index, storage boundaries and remaining profiling work. The first successful load seeds the new display cache; existing build-27 installs do not already have it. Saved content is read-only until verification/fresh section reads complete; no offline write queue or new analytics/backend is introduced.

Final verification and delivery (app/test source `05316b8494c6f0294acbc6f03edb45f3dd8a7855`):

- **Core:** all 91 tests in 12 suites pass on final source (`mfl-build28-core-delivery.log`), including persistent metadata freshness, invalidation and disconnect detachment.
- **Older-iOS focused checks:** all 173 app unit functions and both expanded cached/offline native journeys pass on iOS 18.4 (`mfl-build28-browsing-final.xcresult`). Coverage includes disabled cached lineup after successful auth while its fresh read is held, cleared stale player clocks, revoked/changed franchise handling and player-detail connection messaging without an enabled watch action or stuck spinner.
- **Measured scope:** cached model content became available in about 10 ms with synthetic authentication deliberately held. Two synthetic repository launches reuse one league download while still making two fresh membership checks. These are not phone/network timing guarantees.
- **Native review:** actual cached Scores/Lineup/My Team and maximum-text offline screenshots inspected; compact status stays in scrolling content below navigation, not over the toolbar. Relative timestamps replace detailed dates. Broader Scores/Lineup maximum-text layout work remains a release gate.
- **Full final local regression:** all 173 app unit functions and all 39 native UI journeys pass on the exact final source with Xcode 27 beta / iOS 27 (`mfl-build28-final-full.xcresult`): zero failures, 1,048 seconds for UI. This includes the two-week Preview flow, cache/lineup/trade/Board/My Team/standings/player/IR regressions and final cache presentation fixes. Final startup screenshots were exported and inspected.
- **Phone:** signed build 28 from the final source installed and launched successfully on Josh's iPhone September 7. A read-only file listing confirms a populated approximately 58 KB display cache, 20 KB protected league cache and existing 239 KB daily public catalog (`mfl-build28-phone-cache-files.json`). Only file metadata was inspected, not private response contents. This verifies delivery/cache creation, not measured phone launch latency. No live league mutations were used for QA.
- **Physical profiling:** Instruments could not establish a recording connection after two attempts; the empty trace contains no launch measurements. CoreDevice confirms app 0.5.3 / build 28 installed (`mfl-build28-installed.json`) and running. The trace failure is not counted as performance evidence.
- **GitHub compatibility:** both checks in [run 34087874093](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34087874093) passed on exact head `05316b8`: **91 core tests, all 173 app unit functions and all 39 native UI journeys**, zero failures, Xcode 16.4 / iOS 18.5. The iOS job completed in 26 minutes. Initial run `34087338389` on `92daa36` was superseded, not counted as the final pass.
- **GitHub delivery:** [PR #7](https://github.com/biggsjm/mfl-blitz-ios/pull/7) squash-merged as [d908042](https://github.com/biggsjm/mfl-blitz-ios/commit/d90804282fbdc64447044a72ebe935402c482f52) September 7 at 1:13 AM Central, after exact-head checks passed. The merged tree matches the tested/installed source; the completion record changes documentation only.
- **Documentation:** README, plan/remaining work, API/cache contract, product, privacy/security, package guidance and owner checklist updated for the private-storage change.

Initial development checks exposed a foreground-week cache regression and a test error-assertion compile issue; both were corrected. Failed/superseded runs are not counted as final passes. Device Instruments traces, real Wi-Fi/cellular launch measurements and live Week 1 validation remain open.

## Previous installed private build — 0.5.3 (27)

The [lineup projection card](lineup-projections.md) places status beside kickoff locks below the divider and adds a signed green/orange projected margin against the selected week's opponent. Edited starters recalculate it immediately; incomplete, wrong-week, failed or ambiguous opponent data omits it. No new API calls, persistence, permissions or mutation behavior.

- **Design:** an independent iOS design-review agent inspected real native screenshots and approved the revised card's alignment, score/margin hierarchy, full opponent name and plain metadata footer. This is AI-assisted review, not human user testing.
- **Local tests:** all 165 app unit functions pass (`mfl-build27-models.xcresult`, iOS 27). The final design passes 16 focused lineup model functions and both new native journeys in Light Mode (`mfl-build27-design-light.xcresult`, iOS 18.4); both journeys also pass in Dark Mode with Increase Contrast enabled (`mfl-build27-design-dark-contrast.xcresult`). A further largest-text capture passes (`mfl-build27-ax-upper.xcresult`). No automated tests submit live league changes.
- **Final iOS 27 follow-up:** all 165 app unit functions plus the two new card journeys and existing QB cancellation/replacement journey pass on `bb182d5` — 168 functions / 204 executions, no failures, skips or runtime warnings (`mfl-build27-ios27.xcresult`). Actual screenshots were inspected on both OS versions, including the full largest-text opponent name and footer. The custom light-surface margin colors calculate to 6.33:1 (green) and 5.68:1 (orange) against white; manual full-app assistive-technology review remains open.
- **Full native regression:** all 37 native iOS 27 journeys pass on `bb182d5`, with no failures, skips or runtime warnings (`mfl-build27-ui-regression.xcresult`). This includes the two-week Preview journey and existing lineup/FLEX, trade, waiver, Board, My Team, standings, player and IR safeguards. Together with the full unit suite above, all 165 app unit functions and 37 native journeys pass on the final source.
- **Device:** signed **0.5.3 (27)** installed and launched on Josh's iPhone; `devicectl` confirms the exact version/build (`mfl-build27-installed.json`). This verifies delivery, not live Week 1 projection accuracy.
- **Initial GitHub run:** [PR #6](https://github.com/biggsjm/mfl-blitz-ios/pull/6), [run 34080797912](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34080797912), passed core/build, all app unit tests and 36 of 37 native journeys on `bb182d5`. The existing empty-trade-inbox test checked Resume's hit target immediately after canceling an alert, before iOS 18 finished dismissing it. The following discard flow worked and both new card journeys passed. The test now awaits the actual interactive inbox state and still asserts alert dismissal, draft retention and final discard; app code is unchanged. The final exact-head success is recorded below. The earlier standings/direct-tools increment is already merged in PR #5 below.
- **Timing-regression follow-up:** the corrected trade journey passes three consecutive iOS 18.4 iterations (`mfl-build27-trade-dismissal.xcresult`). Interim test head `40384a4622fd39160c2e1d01f2503fc2c5f141cb` changes only that UI test and documentation after the installed app source `bb182d5`. Compatibility [run 34082262449](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34082262449) was superseded/canceled, not counted as a pass.
- **Additional older-iOS sweep:** the iOS 18.4 full sweep exposed the same premature-navigation pattern in the existing Board reply-draft test: its generic Back tap reported an invalid hit point while the composer was closing, although Resume reply confirmed that the draft was saved. The test now waits for composer dismissal and targets the interactive thread-specific Back control before checking the Board draft. No app logic or assertions were removed. Targeted repeat verification and final compatibility results follow below; an interrupted/superseded run is not a pass.
- **Final GitHub verification:** both checks in [run 34082686774](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34082686774) passed on exact head `f8682ea605757a681488d1671b78e2858cd4d582`: **88 core tests, all 165 app unit functions and all 37 native UI journeys**, zero failures, Xcode 16.4 / iOS 18.5. This includes the Board test correction. App source remains `bb182d5`, matching installed build 27. The earlier already-failed local older-iOS sweep was stopped for targeted verification, not counted as a pass.
- **Final modal repeats:** both corrected Board/trade journeys pass all three consecutive iOS 18.4 iterations each — six executions, no failures (`mfl-build27-modal-repeats.xcresult`). These checks await actual UI state, not fixed sleeps, and retain save, cancel, resume and discard assertions.
- **GitHub delivery:** [PR #6](https://github.com/biggsjm/mfl-blitz-ios/pull/6) was squash-merged as [0442f11](https://github.com/biggsjm/mfl-blitz-ios/commit/0442f11975f68fcb8a3599cc508feaf25859ab76) September 6 at 11:49 PM Central after both exact-head checks passed. The completion record updates documentation only; tested app and test code are unchanged. The roadmap and Week 1 owner checklist retain live-game, distribution and broader accessibility gates.

Early development checks caught a test-fixture saved-status assumption and excessive inherited List icon spacing; both were corrected, not treated as passing final evidence. At the owner's request, the initial passing card was then revised following independent design review. The surrounding Lineup player rows/Starting header still need a separate maximum-Dynamic-Type follow-up, recorded in the roadmap.

## Previous installed private build — 0.5.3 (26)

The approved [standings pattern](standings-pattern.md) is implemented: shared `6–2 · 1st in Warner` headers, actual league-name fallback, correctly scoped division/overall places, confirmed ties and no manufactured preseason place. Tapping the header opens the matching standings context. The selector stays visible; maximum Dynamic Type gets stacked headers and rows. Existing direct tools and mutation protections remain unchanged.

A read-only authenticated request through the app's own session verified that Champion Hall's standings rows were in franchise-ID order with all-zero preseason results and no explicit rank. The earlier array-index implementation was wrong. Build 26 instead compares configured PCT/H2H/PTS/DIVPCT criteria, with separate division scopes, exact record percentages, confirmed pairwise H2H, conservative missing/cyclic states and cached reads. MFL's report remains authoritative for manual/custom orders not exposed in the API; completed-week comparison remains an owner gate. Temporary diagnostic code is removed from the final source; no credentials or authenticated payloads are committed.

Verification:

- **Core:** all **88 tests / 12 suites** pass (`mfl-standings-core-final4.log`), including 13 ranking tests. The owner-requested T2/T2/4 regression proves those places require known points and cannot be inferred with missing points or an earlier unresolved H2H criterion.
- **Focused native/model checks:** **31 app model functions and 5 native UI journeys** pass on iOS 18.4 in Dark Mode (`mfl-build26-standings-verified.xcresult`). Covers header navigation, division/overall ranks, ties, no divisions, largest text, info popover, existing draft navigation and return to IR after player inspection.
- **Final model follow-up:** all 31 My Team/mutation-fixture functions pass on the final app source (`mfl-build26-final-models.xcresult`), including the unassigned-division sorting fallback added after the focused UI run.
- **Light Mode follow-up:** all 3 focused native standings journeys pass on final source `0736ab1`, iOS 18.4, with no failures/skips/runtime warnings (`mfl-build26-standings-light.xcresult`). Actual Light/Dark screenshots were inspected at normal and maximum text sizes.
- **Full local regression:** all **161 app unit functions + 35 native UI journeys** pass: **196 functions / 228 executions**, no failures, skips or reported runtime warnings (`mfl-build26-regression.xcresult`, iPhone 17 Pro / iOS 27). This run started before the two-line unassigned-division sorting fallback; the final model and Light Mode follow-ups above cover that final app source. Includes existing lineup/waiver/trade/board safety, direct tools, IR and the two-week Preview journeys.
- **Device:** signed **0.5.3 (26)** installed and launched on Josh's iPhone; `devicectl` confirms version 0.5.3 / bundle version 26. Production source is `8adfa30`; `0736ab1` adds only the tie-safety test and documentation. No live league mutations were made by QA.
- **GitHub / merge:** final compatibility [run 34078121031](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34078121031) passed on `0736ab12ca69b161da962146f39e0a8f0f3a180f`: 88 core tests, 161 app unit functions and 35 native UI journeys, zero failures, Xcode 16.4 / iOS 18.5. [PR #5](https://github.com/biggsjm/mfl-blitz-ios/pull/5) was squash-merged as [08c97c1](https://github.com/biggsjm/mfl-blitz-ios/commit/08c97c1e588928ad9f37512e2f04fd86f1a5ce60) September 6 at 10:30 PM Central after exact-head checks passed. Superseded run 34078021145 was canceled, not counted as a pass.
- **Documentation:** all 22 local Markdown files pass link checks; whitespace checks pass. Package API/ranking notes, product/navigation/cache/privacy/security docs, roadmap and owner checklist are reconciled. This completion record changes documentation only, not the tested app.

Interim builds 24–25 were installed for presentation and read-only diagnosis, not released, and have been replaced by build 26. Earlier failed or partial runs are not final-source evidence: numeric formatting, a scroll-away selector and a large-text header were corrected before the focused passing run above.

## Verified direct-tool baseline — 0.5.3 (23)

The [direct My Team tools](my-team-shortcuts.md) are implemented: Schedule first, then Adds / Drops, Trades, Watchlist, Injured Reserve and League Activity. Transactions and Manage roster no longer overlap as umbrella pages. Position-grouped season points, cached reads, drafts and reviewed mutation protections remain. All six navigation glyphs use the green accent; contextual IR action glyphs stay neutral. The earlier standing-rank assumption is superseded by build 26 above.

Signed **0.5.3 (23)** from `9a8ab0c9c2eebf949fca692adada1031bf1a035c` installed and launched successfully on Josh's iPhone September 6. Build 22 was installed/launched earlier the same evening; build 23 corrects only the IR shortcut tint and increments the build number. No live league mutation is part of QA; all write tests use explicit Preview. The next feature queue and Week 1/Week 2 release gates are unchanged.

Verification:

- **Core:** 75 tests / 11 suites pass in a fresh temporary SwiftPM directory (`mfl-build22-core.log`). No core source changes in this increment.
- **Full local regression:** all 156 app unit functions and all 33 native UI journeys pass: **189 functions / 221 executions**, no failures, skips or reported runtime warnings (`mfl-build22-regression.xcresult`, iPhone 17 Pro / iOS 27, app/test baseline `82c14b9`). Includes the two-week synthetic journey, draft isolation, independent destinations, maximum Dynamic Type, acquisition reviews, locked Add and eligible IR/readback.
- **Older-iOS Dark Mode:** all three targeted My Team/acquisition/IR journeys pass on iPhone 16 Pro / iOS 18.4, with no failures, skips or reported runtime warnings (`mfl-build22-dark18-final.xcresult`, `9858d58`). That test-only commit tolerates subpixel rounding of a 44-point target; app controls are unchanged.
- **Build-23 follow-up:** both native My Team and eligible-player-action journeys pass in actual Dark Mode on iOS 18.4, no failures/skips/runtime warnings (`mfl-build23-shortcuts.xcresult`, final source `9a8ab0c`). Actual screenshots verify green IR navigation versus neutral contextual IR, along with preserved draft navigation.
- **GitHub:** baseline compatibility [run 34074254837](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34074254837) failed one native IR-navigation test after returning to a scrolled My Team list; core/build/app-model checks passed. The test now scrolls toward the header to reveal the lazy shortcut, without weakening eligibility assertions. Its focused iOS 18.4 rerun passes in build 26. Final compatibility success and [PR #5](https://github.com/biggsjm/mfl-blitz-ios/pull/5) merge are recorded in the build-26 receipt above.
- **Device:** signed build 23 installed and launched successfully. This verifies delivery, not a complete live game week or real owner mutation.
- **Documentation:** current product/navigation/API/cache contracts, direct-tool design, remaining feature plan and owner checklist reconciled. Local Markdown links and whitespace checks pass.

Development findings corrected before final verification: explicit button accessibility for the shortcut grid, independent actions instead of multiple List-row links, a shared typed path for deeper schedule/player navigation, and separate unavailable-report versus no-eligible-player IR states. Earlier failed/interrupted runs and superseded CI runs are not passing evidence. Final documentation completion changes no tested app code.

## Previous installed private build — 0.5.2 (21)

**0.5.2 (21)** installed on Josh's iPhone September 6 from source `e1fcba5`. It adds symbol-only person-plus Add, red person-minus Drop and neutral medical-bag IR for eligible owned players, strict acquisition gating, and My Team's official standing/navigation-card/position-roster redesign with batched season-to-date scores. No live roster changes are part of QA. Installation is confirmed; iOS refused automatic launch because the phone was locked. Open the installed app after unlocking.

The tested source was squash-merged in [PR #4](https://github.com/biggsjm/mfl-blitz-ios/pull/4) as [d8e472b](https://github.com/biggsjm/mfl-blitz-ios/commit/d8e472b934478448dc85b6c18d8c788158310c15) on September 6 after both checks passed. The final completion record changes documentation only. Remaining Week 1/Week 2 gates below are unchanged.

Build-21 verification:

- **Core:** 75 tests / 11 suites passed in a fresh temporary SwiftPM directory (`mfl-player-actions-core-clean.log`). Core source is unchanged by this increment.
- **Full local regression:** all 150 app unit functions and all 32 native journeys passed: **182 functions / 214 executions**, no failures, skips or reported runtime warnings (`mfl-build21-regression.xcresult`, iPhone 17 Pro / iOS 27).
- **Dark Mode follow-up:** all four targeted My Team/Add/IR/locked-player journeys passed without failures, skips or reported runtime warnings (`mfl-build21-dark.xcresult`). Actual Light/Dark screenshots were inspected for the matching cards, position points, neutral medical bag, red person-minus, available/locked person-plus and reviewed cancellation.
- **GitHub compatibility:** [run 34070135205](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34070135205) passed on `e1fcba5`: 75 core tests, all 150 app unit functions and all 32 native UI journeys, zero failures, using the Xcode 16.4 / iOS 18.5 CI environment. The exact tested head was checked before merge.
- **Coverage:** strict acquisition flags and owner scope; one batched cached YTD read instead of assignment fetching; zero/negative/missing totals and position ordering; all three My Team destinations; lineup/trade draft preservation; symbol action names and minimum touch targets; eligible-only IR; locked Add; explicit review/cancellation; watchlist and the two-week synthetic journey.
- **Visual development findings:** corrected duplicate card chevrons, multiple navigation links sharing one List row, and section identifiers overriding player-row identifiers. Explicit compact frames fixed native single-control sizing. A single-action List row can expose a larger accessibility hit area than its drawn circle; tests retain minimum touch-target checks and screenshot verification without treating that larger hit area as an oversized visual button. Superseded/interrupted runs are not passing evidence.
- **Device:** final signed 0.5.2 (21) built successfully and installed on Josh's iPhone. Automatic launch was denied by the lock screen, not by build/signing. No live owner action or completed game week is certified by this installation.
- **Documentation:** product/navigation/API/cache contracts, feature plan, remaining release gates, changelog and owner checklist reconciled; local Markdown links and whitespace checks pass.

## Previous installed build — 0.5.1 (20)

**0.5.1 (20)** implements the approved [features 1–5](player-tools-plan.md): official availability, on-demand league-scored history, synced watchlist, reviewed FCFS add/drop and IR activation/deactivation. The final signed compatibility rebuild installed and launched on Josh's iPhone September 6. Production app code is from `4cab25e`; subsequent commits through `679afeb` change only UI tests. It replaces **0.5.0 (19)**, on which Josh reported enabled IR controls for ineligible players and 429 cooldowns on Lineup/My Team. Build 20 adds early IR eligibility gating, section-local refresh, cached browse reads, deferred history, per-host cooldowns and 1.25-second request spacing. Installation/launch is verified; live post-fix browsing and intended roster moves remain owner checks.

Wire validation: Josh supplied abilities, empty/singleton watchlist responses and player 9431's owned status for franchise 0008. Public availability feeds were checked; last season's nonempty points-allowed response verified position totals. No real roster/trade/waiver/lineup/board writes were made by automated QA. Watching another owner's player is valid and does not imply free-agent eligibility.

Build-20 verification:

- **Core:** 75 tests / 11 suites passed, including optional acquisition flags and host-specific cooldown behavior.
- **Full local app regression:** all 145 app unit functions and 29 native journeys passed: **174 functions / 206 executions**, zero failures, skipped tests or reported runtime warnings (`mfl-player-tools-full-compat-final.xcresult`, iPhone 17 Pro / iOS 27). This validates final production app code; the later test-only corrections receive their own follow-up.
- **Older-iOS journeys:** all five player-tools journeys passed on iOS 18.4 (`mfl-player-tools-ios18-final-journeys.xcresult`). Its two failing legacy journeys were then corrected and both passed (`mfl-player-tools-ios18-transition-retry.xcresult`): trade research/draft cancellation/resume and the two-week preview journey. No tests were skipped. Together these cover all seven targeted older-iOS journeys.
- **Final test-harness follow-up:** all seven corrected native journeys also passed on iOS 27 (`mfl-player-tools-final-ui-harness.xcresult`), with zero failures, skips or reported runtime warnings. This verifies the test-only changes after the full regression above.
- **Final menu-touch regression:** all five player-tools journeys passed on both iOS 27 and iOS 18.4 on `679afeb` (`mfl-player-tools-ios27-visible-menu.xcresult` and `mfl-player-tools-ios18-visible-menu.xcresult`), with zero failures, skips or reported runtime warnings. Coverage explicitly exercises the measured-touch fallback and scrolls full targets clear of bars at largest text. Before/after screenshots verify the actual roster control opens its menu with ineligible IR disabled.
- **Visual inspection:** actual Light/Dark screenshots reviewed for watchlist, on-demand research, explicit add/drop review, IR confirmation, disabled IR in both Player Detail and the roster menu, and largest-text identity/action. Mixed navigation styles and a task attached to initially empty research content were found and fixed; the corresponding research/return journeys now pass.
- **Baseline, not final-source evidence:** the full build-19 local regression passed 170 app/UI functions / 199 executions before owner-feedback fixes. The later focused results above do not mislabel that older run as build 20.
- **Compatibility findings:** earlier CI exposed native-menu test-target differences. iOS 18 wraps the real menu button in an accessibility element with misleading hit-test results. Tests now target the real control, await composer dismissal before reopening, and scroll the entire tiebreaker row above the sticky submit bar. All eligibility, cancellation, saved-draft and exact readback assertions remain. A stale simulator test-runner installation was restarted separately; it was not counted as an app assertion failure.
- **Earlier GitHub compatibility failure:** run [34064800920](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34064800920) passed core, all 145 app unit functions and 26 of 29 native journeys on `ab2277e`. Three roster-menu journeys reported no accessibility hit point for a visible 44-point UIKit control. The corrected helper uses a real touch at its measured center only when the complete enabled target is inside unobscured content; all menu, eligibility, cancellation and exact readback assertions remain. An additional iOS 27 large-text viewport-guard failure was corrected by scrolling before tapping. A separate beta-runner log-finalization stall was interrupted, not counted as a pass.
- **Final GitHub verification:** [34066572620](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34066572620) passed on `679afeb`: **75 core tests, all 145 app unit functions and all 29 native UI journeys**, zero failures, on Xcode 16.4 / iOS 18.5. It includes the measured-touch fallback and largest-text visibility checks. Superseded runs `34065998956` and `34066135043` were cancelled and were not used as passing merge evidence.
- **GitHub delivery:** [PR #3](https://github.com/biggsjm/mfl-blitz-ios/pull/3) was squash-merged as [b4c9cf9](https://github.com/biggsjm/mfl-blitz-ios/commit/b4c9cf9b94d7e3ebebfa2bed3a5f0378cf397eeb) on September 6 after both final checks passed. The completion record updates documentation only; app code and tests remain the verified versions above. Features 1–5 are engineering-complete; actual intended writes, live game-week certification and wider distribution gates remain open.

Default request spacing reduces pressure but does not guarantee freedom from MFL's variable per-server/per-IP limits. Registered client identity remains unconfirmed. Native IR is conservatively limited to current Out/IR reports verified for Champion Hall; the API does not expose every league-specific IR rule, and MFL still enforces final eligibility.

## Earlier installed private builds

- **MFL Blitz 0.4.1 (18): My Team and Board drafts.** Implemented, tested and signed. The final compatibility rebuild was installed and launched successfully on Josh's iPhone on September 6 after the phone became available. Includes the concise schedule update label and Board Close/save/discard with visible draft recovery.
- Previous installed baseline: **0.3.7 (16), [e779e4b](https://github.com/biggsjm/mfl-blitz-ios/commit/e779e4b)**. The documentation baseline was merged in [PR #1](https://github.com/biggsjm/mfl-blitz-ios/pull/1).
- **Private owner testing**, not an App Store/TestFlight release. Production MFL registration and Week 2 invitation readiness remain unconfirmed.
- Tabs remain **Scores / Lineup / My Team / Standings / Board**. Build 22 exposes six Schedule-first tools directly inside My Team. Build 34 moves Settings to My Team; Scores and Lineup retain Week N controls.
- Connected mode performs real reads and explicitly reviewed writes. Champion Hall preview uses synthetic data, offers, owners and schedules; its actions send nothing to MFL.

## Implemented workflows

| Area | Behavior | Important boundary |
| --- | --- | --- |
| Scores | Featured/all matchups, positional/FLEX detail, foreground refresh and final reconciliation | No background push; projections are not live win forecasts |
| Lineup | League-aware bench/starter/FLEX replacements and rotations, saved drafts, review and starter readback | MFL stores starter IDs, not FLEX placements; saved tiebreakers cannot be read back |
| My Team / team pages | Official own-team standing, six Schedule-first tools, then position-grouped roster sorted by actual season points | Missing totals stay absent; other teams retain assignment-based rosters and never show your inbox or owner controls |
| Player Detail | Identity, ownership/status, one Week card, season totals/average, visible points/opponent log, watch star and lazy biography | Missing source data stays absent; historical NFL opponents use the explicitly disclosed current-team approximation; no external imagery/news/raw-stat provider |
| Season schedule | Shared team/league timelines, configured bounds, current/future/final/unset states and route-local matchup scoring | Ambiguous doubleheaders never select an arbitrary scoring game; missing opponents are not guessed byes |
| Adds / Drops and Injured Reserve | One available-player browser for FCFS or blind bids; owned-player drops; dedicated IR capacity/eligible moves, all with exact membership readback | Supported owner/rule gates only; broader classic/non-conditional, taxi, salary and duplicate-player formats use MFL |
| Trades | Create/Resume, private drafts, Cancel/rollback, blank-save disabled, exact proposal/response review and separate counters | Counter leaves original open; acceptance can await MFL processing; no blind retries |
| Standings | Official overall/division order, owners, artwork, anchored info and team drill-through | Missing owners not guessed; current record is not a projected record |
| Board | Native threads/posts/replies, visible Drafts, Resume reply, Close/save/discard and exact-post readback | Saving a draft sends no post; plain text, not rich HTML or proprietary chat |
| Storage/network | Device-only Keychain sessions/drafts/markers, daily public catalog, protected league metadata/display snapshots and other private memory caches | Cached display cannot authorize changes; no full offline browsing, analytics or backend |

Team/player/season reads have session-generation and canonical-route checks. Browsing never changes the active lineup draft, shared scoring week or saved trade terms. Current schedule detail replaces the scoreboard poller while visible; future weeks do not poll. League metadata and public player data remain cached during roster/player refresh.

## Historical My Team / build-18 verification evidence

- **MFLCore:** 68 tests in 10 suites passed in a fresh temporary build directory, including schedule wire/request/cache/auth regressions.
- **My Team full native regression:** 147 app/UI functions / 173 executions passed, including all 22 then-existing native UI journeys, with zero failures or reported runtime warnings on iPhone 17 Pro / iOS 27 simulator.
- **My Team follow-up:** all 126 app unit functions plus the current-schedule native journey passed (127 functions / 153 executions), after concise freshness, duplicate-source-ID safety and final-results rollover adjustments. This adds one freshness test to that full-suite baseline.
- **Build 18 final regression:** all 130 app unit functions plus three Board native journeys passed (133 functions / 159 executions), with zero failures or runtime warnings. Covers blank close, Keep editing, save/resume from Drafts, reply recovery, discard/no resurrection, team isolation, storage failures and posting cleanup. The final project contains 130 unit functions and 24 UI journeys; GitHub runs the complete suite. MFLCore remains 68 tests / 10 suites.
- **Compatibility follow-up:** all 130 app unit functions and both player-navigation UI journeys passed after the task-group and accessibility changes (132 functions / 158 executions), zero failures/runtime warnings on the local iOS 27 simulator. All three Board journeys also passed a separate Dark Mode run. The corrected [Xcode 16.4 / iOS 18.5 CI run](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34054161224) then passed all 130 app unit functions and all 24 UI journeys, plus the separate core job.
- **Coverage:** cache request budgets, stale-account completion, multiple ownership, schedule bounds/duplicates and four synthetic managers across two accelerated weeks. My Team → roster → player → future matchup preserves an edited lineup; current Week 1 matchup → player preserves a separate Week 2 selection. Standings → team → league schedule, maximum Dynamic Type Transactions access, lineup arrows and standings information also passed.
- **Visual inspection:** actual native screenshots reviewed for My Team roster/schedule, player ownership/projection, future-matchup missing-score behavior, league schedule, lineup action separation, tab artwork and large-text trade access. Default-size Light and Dark appearances verified; complete manual accessibility/device coverage remains open.
- **Physical device:** the final signed 0.4.1 (18) compatibility rebuild installed and launched successfully on September 6 once Josh made the phone available. Earlier attempts were blocked by locking/connectivity, not build/signing errors. This confirms delivery and launch, not a complete physical-device workflow audit. Josh reported the earlier My Team screen looked good and supplied a live schedule screenshot; that observation does not certify all opponent data or a full game week. Board's latest native composer, centered close prompt and readable Drafts entry were visually reviewed in Light and Dark appearances in the simulator. No live league writes were performed for installation or launch verification.
- **GitHub:** [PR #2](https://github.com/biggsjm/mfl-blitz-ios/pull/2) was squash-merged as [632a4a6](https://github.com/biggsjm/mfl-blitz-ios/commit/632a4a66dccf5c98f36be10e71d2809fcdd037bc) on September 6 after both core and iOS checks passed for final source revision `2cc036e`. This documentation-only completion record changes no app code or tests. The PR and [CI history](https://github.com/biggsjm/mfl-blitz-ios/actions/workflows/ci.yml) retain the detailed check results.

Development artifacts are outside the repository. Intermediate failures were corrected before final verification: a test fixture initially rejected empty synthetic credentials; a root accessibility identifier hid child identifiers; the beta simulator briefly exposed duplicate tab entries. A synthetic own-team scoring-ID mismatch also required preview-only detail lookup coverage. These earlier failed runs are not counted as passing final tests.

The first complete pre-merge [CI run](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34052528470) found additional Xcode 16.4 / iOS 18.5 compatibility failures: roster tests aborted during Swift task allocation cleanup, and two player-page UI assertions depended on literal section-heading capitalization. Roster assembly now uses a bounded task group; player ownership uses explicit heading/row identifiers and stable text casing. Existing regression cases remain enabled. The corrected full run passed before merge; neither the failed run nor local beta-toolchain success alone was used as the merge gate.

All native QA explicitly enters preview and performs no live lineup, waiver, trade or board writes. Synthetic managers/weeks are automated scenarios, not human participants or real elapsed game weeks.

## Historical baseline and owner evidence

- Build 16: 96 local app/UI functions / 121 executions; full app unit suite plus five trade journeys, zero failures/runtime warnings. Its [app CI](https://github.com/biggsjm/mfl-blitz-ios/actions/runs/34046368810) passed core and iOS jobs.
- Earlier build 0.3.3 benchmarks and methods remain in the [synthetic report](two-week-synthetic-testing.md); [starter/FLEX behavior](lineup-starter-swaps.md) retains its versioned evidence.
- Josh reported successful lineup submission and visible projections. Earlier authenticated read-only device checks confirmed team artwork, owners, catalog reuse and tradable assets.
- Complete actual Week 1 scoring/rollover/processing and consenting-owner trade validation remain open. Dated counts do not guarantee future league data.

## Known gaps and next work

The installed build includes [Player tools 1–5](player-tools-plan.md): availability context, player research, synced watchlists, native FCFS add/drop and IR. Its checkboxes track engineering delivery separately from live-owner certification; trading blocks, calendars, polls and playoff brackets are explicitly queued afterward.

- Compare completed player history, season metrics and points-allowed coverage against MFL after Week 1; the bounded implementation and synthetic fixtures are not live-game evidence.
- Contextual waiver/trade links are implemented. Validate their live-owner use with retained search, selections and drafts. No Players tab/global directory is introduced.
- Season timelines use league bounds, but the separate Scores/Lineup week picker still uses 1…18.
- Optional live biography fields vary. No headshots/news/licensed raw-stat feed or ADP is integrated; official availability and fantasy totals are now implemented.
- Legacy live Lineup `opponent`/game-time and Lineup/Waiver `seasonPoints`/trend/rostered-percent placeholders are not reused as player research facts.
- Broad league-format certification, multi-league switching, broader FCFS/IR formats, taxi/commissioner management, rich board HTML, full offline schedule/player browsing, diagnostics export and iPad split-view remain unfinished. Bounded display-only startup snapshots are implemented in build 28; real-device profiling remains open.
- Full manual VoiceOver/Voice Control/Switch Control, contrast, small-screen/iPad and older-supported-OS validation remain release gates.
- MFL client registration is unconfirmed. The app still supplies `MFL Blitz/0.1 (com.biggsjm.MFLBlitz)`; do not change a registered-client identity merely to match a marketing version.
- GitHub private vulnerability reporting was disabled at the September 6 audit. Establish a safe private reporting route before wider distribution.
- Apple/App Store Connect/TestFlight setup, actual Week 1 evidence and Josh's invitation decision remain open. Follow the [owner checklist](week-1-testing.md) and P0 [release gates](roadmap.md); My Team does not replace them.
